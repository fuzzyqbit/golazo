---
phase: 03-publish-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/publish/templates.ts
  - src/publish/templates.test.ts
  - src/publish/templates.test-cases.ts
  - src/publish/index.ts
autonomous: true
requirements:
  - PUB-02
  - PUB-03

must_haves:
  truths:
    - "`renderTitle(input)` produces the documented title template `{Kid} · vs {Opponent} · {scoreFor}–{scoreAgainst} {result} · {date}` with all five substitutions applied — the en-dash between scores is `–` (U+2013), not a hyphen `-`; the bullet separator is `·` (U+00B7); both are pinned by the test suite"
    - "`renderDescription(input)` produces the documented multi-line description template with all nine substitutions ({Kid}, {Opponent}, {scoreFor}, {scoreAgainst}, {result}, {date}, {jersey}, {club}, {source}) applied; line breaks are `\\n` (LF), not `\\r\\n`"
    - "Opponent slugs render pretty via the SAME `prettyOpponent` helper at `src/render/opponentPretty.ts` from Plan 02-04 — `renderTitle({game: {opponent: 'city-sc', ...}})` includes `'City SC'`, `'ac-milan'` becomes `'AC Milan'`, `'united'` becomes `'United'`. The helper is IMPORTED, not redefined."
    - "`renderTitle` + `renderDescription` are pure functions: same input → same output; no I/O; no side effects; no clock/random reads"
    - "Invalid input shapes (missing required field) fail at the TypeScript boundary — `TemplateInput` interface requires all 5 game fields + 4 channel fields; runtime `renderTitle`/`renderDescription` perform a defensive zod parse and throw `TemplateError` on shape violations"
    - "Title length is recorded as a known constraint: YouTube enforces a 100-char limit on video titles; the test suite includes a long-opponent case asserting realistic combinations fit under 100 chars (informational — Plan 03-03 may add truncation if needed, but Plan 03-02 does NOT truncate, so Plan 03-03 sees raw template output)"
  artifacts:
    - path: "src/publish/templates.ts"
      provides: "Pure title + description template renderers — input: { kid: { name, club, jersey, source }, game: { date, opponent, scoreFor, scoreAgainst, result } } → output: { title: string; description: string }"
      exports: ["renderTitle", "renderDescription", "renderTemplates", "TemplateInput", "TemplateOutput", "TITLE_TEMPLATE", "DESCRIPTION_TEMPLATE"]
    - path: "src/publish/templates.test-cases.ts"
      provides: "Exported TEMPLATE_TEST_CASES array — same `*.test-cases.ts` sibling pattern from Plans 01-02 + 01-03 (importable by non-vitest tooling; excluded from dist/ via tsconfig)"
      exports: ["TEMPLATE_TEST_CASES"]
    - path: "src/publish/index.ts"
      provides: "Barrel extended to re-export the template surface"
      exports: ["renderTitle", "renderDescription", "renderTemplates", "TemplateInput", "TemplateOutput"]
  key_links:
    - from: "src/publish/templates.ts"
      to: "src/render/opponentPretty.ts"
      via: "prettyOpponent import — REUSES Plan 02-04's helper rather than redefining (PUB-03 contract)"
      pattern: "import.*prettyOpponent.*from '\\.\\./render/opponentPretty"
    - from: "src/publish/templates.ts"
      to: "src/publish/errors.ts"
      via: "TemplateError thrown on invalid input shape"
      pattern: "TemplateError"
---

<objective>
Implement the pure title + description template renderers consumed by Plan 03-05's `runPublish` orchestrator:

1. `renderTitle({ kid, game })` — applies the documented title template with the 5 game-side substitutions.
2. `renderDescription({ kid, game })` — applies the documented description template with all 9 substitutions ({Kid}, {Opponent}, {scoreFor}, {scoreAgainst}, {result}, {date}, {jersey}, {club}, {source}).
3. `renderTemplates({ kid, game })` — convenience wrapper returning `{ title, description }` together; this is what Plan 03-05 calls.
4. Table-driven tests covering: standard cases, every opponent-slug shape (hyphenated, acronym allow-list, lowercase singleton), edge cases (`L`/`D` results), and a length sanity check for the title against YouTube's 100-char limit.

Purpose: Closes PUB-02 (all 9 template substitutions) + PUB-03 (opponent slug pretty-printing via title-case + hyphen-to-space + acronym allow-list `sc`, `fc`, `ac`). REUSES `prettyOpponent` from `src/render/opponentPretty.ts` (Plan 02-04) — does NOT redefine the helper. This is the Phase 2 marker noted in the Plan 02-04 SUMMARY: "Phase 3 PUB-03 imports the same helper rather than re-implementing it."

Output: Pure-function template renderers ready for Plan 03-03 (uploader builds `videos.insert` request body using these) and Plan 03-05 (orchestrator wires manifest + channel config into the renderer). No I/O; no SDK calls; runs in microseconds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/superpowers/specs/2026-05-13-golazo-design.md
@.planning/phases/02-render-pipeline/02-04-SUMMARY.md

<interfaces>
<!-- Pinned from source-of-truth files. NodeNext .js extensions. -->

From src/render/opponentPretty.ts (Plan 02-04 — PUB-03 reuses, does NOT redefine):
  export const ACRONYM_ALLOW_LIST = new Set(['sc', 'fc', 'ac']);
  export function prettyOpponent(slug: string): string;
  // Examples:
  //   'united'         → 'United'
  //   'city-sc'        → 'City SC'
  //   'ac-milan'       → 'AC Milan'
  //   'real-madrid-cf' → 'Real Madrid Cf'  (cf NOT in allow-list)
  //   ''               → ''

From docs/superpowers/specs/2026-05-13-golazo-design.md (the canonical template source):

  TITLE_TEMPLATE = `{Kid} · vs {Opponent} · {scoreFor}–{scoreAgainst} {result} · {date}`
  // NOTE: bullet separator is U+00B7 MIDDLE DOT, NOT an ASCII period.
  // NOTE: score separator is U+2013 EN DASH, NOT an ASCII hyphen-minus.
  // These two character choices are visual-design decisions in the spec.

  DESCRIPTION_TEMPLATE =
    'Match Day · {date}\n' +
    '{Kid} (#{jersey}, {club}) vs {Opponent}\n' +
    'Final: {scoreFor}–{scoreAgainst}\n' +
    '\n' +
    'Filmed via {source}. Edited with golazo.'

<!-- Contracts produced by this plan (frozen — Plans 03-03 + 03-05 consume). -->

src/publish/templates.ts:

  export interface TemplateInput {
    kid: {
      name: string;        // ChannelConfig.name (e.g. 'Leo') — substitutes {Kid}
      club: string;        // ChannelConfig.club (e.g. 'FC Eagles') — substitutes {club}
      jersey: number;      // ChannelConfig.jersey (1..99 integer) — substitutes {jersey}
      source: string;      // ChannelConfig.source (e.g. 'Veo' | 'Trace') — substitutes {source}
    };
    game: {
      date: string;        // YYYY-MM-DD — substitutes {date}
      opponent: string;    // RAW slug from manifest.game.opponent (e.g. 'city-sc') — prettyOpponent is applied inside renderTitle/renderDescription, so callers pass the raw slug
      scoreFor: number;    // integer >= 0
      scoreAgainst: number;// integer >= 0
      result: 'W' | 'L' | 'D';
    };
  }

  export interface TemplateOutput {
    title: string;
    description: string;
  }

  export const TITLE_TEMPLATE: string;        // exported for documentation / smoke tests
  export const DESCRIPTION_TEMPLATE: string;

  export function renderTitle(input: TemplateInput): string;
  export function renderDescription(input: TemplateInput): string;
  export function renderTemplates(input: TemplateInput): TemplateOutput;
  // Convenience: returns { title: renderTitle(input), description: renderDescription(input) }.

<!-- TemplateError contract — additive on src/publish/errors.ts (already created in Plan 03-01) -->

src/publish/errors.ts (additive — append to Plan 03-01's file):
  export class TemplateError extends Error {
    // Single-line message: "template: <field>: <reason>. <remediation>"
    // Thrown on runtime shape violations (defensive — TypeScript catches most at compile time).
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TemplateError class + template renderers with table-driven tests</name>
  <files>src/publish/templates.ts, src/publish/templates.test.ts, src/publish/templates.test-cases.ts, src/publish/errors.ts, src/publish/index.ts</files>
  <read_first>
    - docs/superpowers/specs/2026-05-13-golazo-design.md (section: Data Flow > Templates — pin the exact bullet + en-dash characters, the line ordering, and the 9 substitution keys)
    - .planning/REQUIREMENTS.md (PUB-02 — 9 substitutions; PUB-03 — opponent pretty-print via title-case + hyphen-to-space + `sc`/`fc`/`ac` allow-list)
    - src/render/opponentPretty.ts (Plan 02-04 — REUSE; do NOT redefine; do NOT copy the function body)
    - src/render/opponentPretty.test.ts (Plan 02-04 — the 8 cases pinning `prettyOpponent` behaviour; this plan adds the orthogonal axis of template assembly on top)
    - src/publish/errors.ts (Plan 03-01 — OAuthError lives here; TemplateError extends the same file following the same single-line-message convention)
    - src/publish/errors.test.ts (Plan 03-01 — extend with TemplateError cases; do NOT delete the 4 OAuthError cases)
    - src/publish/index.ts (Plan 03-01 — barrel re-exports OAuth helpers; extend with template re-exports)
    - src/config/channels.test-cases.ts (Plan 01-02 — pattern for the `*.test-cases.ts` sibling: typed exported const array, importable from `.test.ts`, excluded from dist via the base tsconfig include set — `src/**/*.test-cases.ts` is currently in include? — confirm by reading the file's location relative to tsconfig include; the existing channels case file proves the pattern works)
  </read_first>
  <behavior>
    src/publish/errors.ts — additive TemplateError class:

      export interface TemplateErrorInput { field: string; reason: string; remediation: string; }
      export interface TemplateErrorJson { name: 'TemplateError'; field: string; reason: string; remediation: string; }
      export class TemplateError extends Error { /* "template: <field>: <reason>. <remediation>" */ }

    src/publish/errors.test.ts — append 4 cases (preserve the 4 OAuthError cases from Plan 03-01):

      5. TemplateError message format: `new TemplateError({ field: 'game.opponent', reason: 'empty string', remediation: 'check manifest' }).message === "template: game.opponent: empty string. check manifest"`
      6. `instanceof TemplateError` and `instanceof Error` both true
      7. `toJSON()` returns the expected shape with `name: 'TemplateError'`
      8. `err.name === 'TemplateError'`

    src/publish/templates.test-cases.ts — exported `TEMPLATE_TEST_CASES` array of `{ name: string; input: TemplateInput; expected: TemplateOutput }`:

      Case 1 — Plan 02-04 fixture parity (leo vs united 3-1 W):
        input: { kid: { name: 'Leo', club: 'FC Eagles', jersey: 10, source: 'Veo' }, game: { date: '2026-05-13', opponent: 'united', scoreFor: 3, scoreAgainst: 1, result: 'W' } }
        expected.title: 'Leo · vs United · 3–1 W · 2026-05-13'
        expected.description: 'Match Day · 2026-05-13\nLeo (#10, FC Eagles) vs United\nFinal: 3–1\n\nFilmed via Veo. Edited with golazo.'

      Case 2 — hyphenated opponent (city-sc → City SC):
        input: { kid: { name: 'Mateo', club: 'City SC', jersey: 7, source: 'Trace' }, game: { date: '2026-05-12', opponent: 'city-sc', scoreFor: 2, scoreAgainst: 2, result: 'D' } }
        expected.title: 'Mateo · vs City SC · 2–2 D · 2026-05-12'
        expected.description: 'Match Day · 2026-05-12\nMateo (#7, City SC) vs City SC\nFinal: 2–2\n\nFilmed via Trace. Edited with golazo.'

      Case 3 — acronym in front (ac-milan → AC Milan):
        input: { kid: { name: 'Leo', club: 'FC Eagles', jersey: 10, source: 'Veo' }, game: { date: '2026-06-01', opponent: 'ac-milan', scoreFor: 0, scoreAgainst: 4, result: 'L' } }
        expected.title: 'Leo · vs AC Milan · 0–4 L · 2026-06-01'
        expected.description: 'Match Day · 2026-06-01\nLeo (#10, FC Eagles) vs AC Milan\nFinal: 0–4\n\nFilmed via Veo. Edited with golazo.'

      Case 4 — acronym at end (real-madrid-fc → Real Madrid FC):
        input: { kid: { name: 'Leo', club: 'FC Eagles', jersey: 10, source: 'Veo' }, game: { date: '2026-06-02', opponent: 'real-madrid-fc', scoreFor: 1, scoreAgainst: 1, result: 'D' } }
        expected.title: 'Leo · vs Real Madrid FC · 1–1 D · 2026-06-02'

      Case 5 — repeated allow-list (sc-fc-ac → SC FC AC) — proves prettyOpponent is applied per-part:
        input: { kid: { name: 'Mateo', club: 'City SC', jersey: 7, source: 'Trace' }, game: { date: '2026-06-03', opponent: 'sc-fc-ac', scoreFor: 5, scoreAgainst: 0, result: 'W' } }
        expected.title: 'Mateo · vs SC FC AC · 5–0 W · 2026-06-03'

      Case 6 — non-allow-list part (real-madrid-cf → Real Madrid Cf — cf NOT in allow-list):
        input: { kid: { name: 'Leo', club: 'FC Eagles', jersey: 10, source: 'Veo' }, game: { date: '2026-06-04', opponent: 'real-madrid-cf', scoreFor: 2, scoreAgainst: 3, result: 'L' } }
        expected.title: 'Leo · vs Real Madrid Cf · 2–3 L · 2026-06-04'

      Case 7 — single-character opponent (a → A):
        input: { kid: { name: 'Leo', club: 'FC Eagles', jersey: 10, source: 'Veo' }, game: { date: '2026-06-05', opponent: 'a', scoreFor: 1, scoreAgainst: 0, result: 'W' } }
        expected.title: 'Leo · vs A · 1–0 W · 2026-06-05'

      Case 8 — large jersey (jersey 99):
        input: { kid: { name: 'Leo', club: 'FC Eagles', jersey: 99, source: 'Veo' }, game: { date: '2026-06-06', opponent: 'united', scoreFor: 0, scoreAgainst: 0, result: 'D' } }
        expected.title: 'Leo · vs United · 0–0 D · 2026-06-06'
        expected.description: 'Match Day · 2026-06-06\nLeo (#99, FC Eagles) vs United\nFinal: 0–0\n\nFilmed via Veo. Edited with golazo.'

      Case 9 — kid name with special chars (apostrophe — proves no escaping):
        input: { kid: { name: "O'Leo", club: 'FC Eagles', jersey: 10, source: 'Veo' }, game: { date: '2026-06-07', opponent: 'united', scoreFor: 1, scoreAgainst: 0, result: 'W' } }
        expected.title: "O'Leo · vs United · 1–0 W · 2026-06-07"

      Case 10 — multi-word club (proves description's `{club}` does not collide with `{Kid}`):
        input: { kid: { name: 'Leo', club: 'FC Bayern München', jersey: 10, source: 'Veo' }, game: { date: '2026-06-08', opponent: 'united', scoreFor: 3, scoreAgainst: 1, result: 'W' } }
        expected.description: 'Match Day · 2026-06-08\nLeo (#10, FC Bayern München) vs United\nFinal: 3–1\n\nFilmed via Veo. Edited with golazo.'

    src/publish/templates.test.ts — table-driven tests:

      - `it.each(TEMPLATE_TEST_CASES)` with case 1-10: assert `renderTitle(input) === expected.title` AND `renderDescription(input) === expected.description` AND `renderTemplates(input)` deep-equals `expected`.
      - Plus the following non-table cases:

      11. CHARACTER SANITY — title uses U+00B7 MIDDLE DOT (not period): assert `renderTitle(case1Input).includes('\\u00B7')`; assert it does NOT include `' . '` (with surrounding spaces, the period-with-spaces pattern).
      12. CHARACTER SANITY — title uses U+2013 EN DASH (not hyphen): assert `renderTitle(case1Input).includes('3\\u20131')`; assert it does NOT include `'3-1'`.
      13. LINE ENDINGS — description uses LF only: assert `!renderDescription(case1Input).includes('\\r')`.
      14. LENGTH SANITY — title length: for case 5 (longest opponent — `'sc-fc-ac'` → `'SC FC AC'`, plus a long kid name `'Leonardo'`), `renderTitle(...).length <= 100` (YouTube's title cap). This is INFORMATIONAL — Plan 03-03 will decide on truncation policy; this plan does NOT truncate.
      15. PURE — same input twice yields identical output: `renderTitle(case1Input) === renderTitle(case1Input)` (referential, no clock/random).
      16. ROW-COUNT GATE — `expect(TEMPLATE_TEST_CASES.length).toBe(10)` so future additions are explicit.

      All non-table assertions live BELOW the `it.each` block.

      Defensive shape tests (no need for table — TypeScript catches these at compile time but the runtime defensive parse is a contract):

      17. `renderTitle({ kid: { name: '', ... }, ... })` throws TemplateError with field `kid.name` and reason mentioning empty/required (use a tiny inline zod parse inside the implementation; throw TemplateError on `safeParse.success === false`).
      18. `renderDescription({ ..., game: { ..., result: 'X' as any } })` throws TemplateError with field `game.result` and reason mentioning the enum.
      19. `renderTemplates({ ..., game: { ..., scoreFor: -1 } })` throws TemplateError with field `game.scoreFor` and reason mentioning non-negative.
  </behavior>
  <action>
    --- src/publish/errors.ts (additive — extend Plan 03-01's file) ---

    Append `TemplateError` class following the EXACT same shape as `OAuthError`:
      - `TemplateErrorInput`, `TemplateErrorJson` interfaces
      - `class TemplateError extends Error`
      - Constructor message: `template: ${input.field}: ${input.reason}. ${input.remediation}`
      - `name = 'TemplateError'`, `Object.setPrototypeOf`, `toJSON()`
    Total error classes in the file after this: 2 (OAuthError, TemplateError).

    --- src/publish/errors.test.ts (additive — extend Plan 03-01's test file) ---

    Append cases 5-8 per `<behavior>`. The 4 existing OAuthError cases (1-4) are unchanged.

    --- src/publish/templates.test-cases.ts ---

    Export `TEMPLATE_TEST_CASES` as a `readonly Array<{ name: string; input: TemplateInput; expected: TemplateOutput }>`. Use the exact case data from `<behavior>`. The file imports `TemplateInput` + `TemplateOutput` types from `./templates.js` (this creates a circular-ish import only at the type level — TypeScript handles type-only edges cleanly; the channels.test-cases.ts file does the same pattern).

    --- src/publish/templates.ts ---

    Imports:
      ```
      import { z } from 'zod';
      import { prettyOpponent } from '../render/opponentPretty.js';
      import { TemplateError } from './errors.js';
      ```

    Export `TITLE_TEMPLATE` and `DESCRIPTION_TEMPLATE` constants — the literal template strings with `{Kid}` / `{Opponent}` / `{scoreFor}` / `{scoreAgainst}` / `{result}` / `{date}` / `{jersey}` / `{club}` / `{source}` placeholders. Useful for documentation + a smoke test that loads them.

    Export `TemplateInput` and `TemplateOutput` interfaces per `<interfaces>`.

    Inline zod schema for defensive runtime validation:
      ```
      const templateInputSchema = z.object({
        kid: z.object({
          name: z.string().min(1),
          club: z.string().min(1),
          jersey: z.number().int().min(1).max(99),
          source: z.string().min(1),
        }),
        game: z.object({
          date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
          opponent: z.string().min(1),
          scoreFor: z.number().int().min(0),
          scoreAgainst: z.number().int().min(0),
          result: z.enum(['W', 'L', 'D']),
        }),
      });
      ```

    Helper `validateInput(input: TemplateInput): TemplateInput`:
      ```
      const r = templateInputSchema.safeParse(input);
      if (!r.success) {
        const issue = r.error.issues[0];
        const field = issue?.path.map(String).join('.') ?? '(root)';
        throw new TemplateError({ field, reason: issue?.message ?? 'invalid', remediation: 'check manifest + channels.yaml' });
      }
      return r.data;
      ```

    `renderTitle(input)`:
      ```
      const v = validateInput(input);
      const opp = prettyOpponent(v.game.opponent);
      return `${v.kid.name} · vs ${opp} · ${v.game.scoreFor}–${v.game.scoreAgainst} ${v.game.result} · ${v.game.date}`;
      ```
    Note the explicit U+00B7 and U+2013 — written as escape sequences so the source file stays ASCII-safe even if a tool strips non-ASCII. The escape form is what the test cases match.

    `renderDescription(input)`:
      ```
      const v = validateInput(input);
      const opp = prettyOpponent(v.game.opponent);
      return [
        `Match Day · ${v.game.date}`,
        `${v.kid.name} (#${v.kid.jersey}, ${v.kid.club}) vs ${opp}`,
        `Final: ${v.game.scoreFor}–${v.game.scoreAgainst}`,
        '',
        `Filmed via ${v.kid.source}. Edited with golazo.`,
      ].join('\n');
      ```
    Note: validateInput is called separately in both renderers so calling either standalone gets the same defensive check. `renderTemplates` calls validateInput ONCE then both renderers internally re-validate; that's intentional and microsecond-cheap.

    `renderTemplates(input)`:
      ```
      const v = validateInput(input);
      return { title: renderTitle(v), description: renderDescription(v) };
      ```

    --- src/publish/templates.test.ts ---

    Implement all 19 cases (10 table-driven via `it.each(TEMPLATE_TEST_CASES)`, plus cases 11-19 as standalone `it` blocks).

    --- src/publish/index.ts ---

    Extend the Plan 03-01 barrel:
      ```
      export { renderTitle, renderDescription, renderTemplates, TITLE_TEMPLATE, DESCRIPTION_TEMPLATE } from './templates.js';
      export type { TemplateInput, TemplateOutput } from './templates.js';
      export { TemplateError } from './errors.js';
      ```

    RED → GREEN: write tests first; confirm failures; implement renderers.
  </action>
  <verify>
    <automated>cd /Users/me/Documents/code/golazo && npx tsc --noEmit -p tsconfig.check.json && grep -q 'export function renderTitle' src/publish/templates.ts && grep -q 'export function renderDescription' src/publish/templates.ts && grep -q 'export function renderTemplates' src/publish/templates.ts && grep -q "import.*prettyOpponent.*from '\\.\\./render/opponentPretty" src/publish/templates.ts && ! grep -q 'function prettyOpponent' src/publish/templates.ts && grep -q 'export class TemplateError' src/publish/errors.ts && grep -q 'TEMPLATE_TEST_CASES' src/publish/templates.test-cases.ts && npx vitest run src/publish/templates.test.ts src/publish/errors.test.ts && node -e "import('./src/publish/templates.js').then(m => { const out = m.renderTemplates({ kid: { name: 'Leo', club: 'FC Eagles', jersey: 10, source: 'Veo' }, game: { date: '2026-05-13', opponent: 'united', scoreFor: 3, scoreAgainst: 1, result: 'W' } }); if (out.title !== 'Leo \\u00B7 vs United \\u00B7 3\\u20131 W \\u00B7 2026-05-13') { console.error('title mismatch:', JSON.stringify(out.title)); process.exit(1); } if (!out.description.includes('Match Day') || !out.description.includes('Filmed via Veo')) { console.error('description mismatch:', JSON.stringify(out.description)); process.exit(1); } if (out.description.includes('\\r')) { console.error('description has CR'); process.exit(1); } })"</automated>
  </verify>
  <acceptance_criteria>
    - `src/publish/templates.ts` exports `renderTitle`, `renderDescription`, `renderTemplates`, `TITLE_TEMPLATE`, `DESCRIPTION_TEMPLATE`, `TemplateInput`, `TemplateOutput`
    - `prettyOpponent` is imported from `'../render/opponentPretty.js'` — the grep gate `import.*prettyOpponent.*from '\\.\\./render/opponentPretty'` is GREEN; the negative gate `! grep -q 'function prettyOpponent' src/publish/templates.ts` is also GREEN (confirms no redefinition)
    - All 10 table-driven test cases pass; all 9 non-table cases pass (19 total)
    - `src/publish/errors.ts` extended with `TemplateError` (2 error classes total: OAuthError + TemplateError)
    - `src/publish/errors.test.ts` covers both classes (4 OAuthError cases from Plan 03-01 + 4 TemplateError cases = 8 total)
    - `src/publish/index.ts` barrel re-exports template surface so Plans 03-03 + 03-05 import from `'../publish/index.js'`
    - The Node-inline smoke at the end of `<verify>` proves title contains U+00B7 (middle dot) + U+2013 (en dash) and description has no `\r`
    - `npx tsc --noEmit -p tsconfig.check.json` exits 0
    - Pure: `renderTitle` and `renderDescription` are referentially transparent (no clock/random/I/O). Verified by case 15.
  </acceptance_criteria>
  <done>
    PUB-02 and PUB-03 closed. Plan 03-05's orchestrator can call `renderTemplates({ kid: channelConfig, game: manifest.game })` to get a `{ title, description }` ready for `videos.insert`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Templates are pure functions over already-validated inputs from manifest + channels.yaml; no new trust boundary crosses this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | renderDescription embedding sensitive data | accept | The template substitutes ONLY operator-public fields (kid display name, jersey, club, source label, game metadata from folder name). No tokens, no internal IDs, no PII beyond what the operator already chose to publish on YouTube. |
| T-03-09 | Tampering | Injection via opponent slug | mitigate | `prettyOpponent` only lowercases + splits on hyphens + title-cases — it cannot introduce newlines or YouTube-special chars from an opponent slug because the slug regex (enforced upstream by Plan 01-03's `parseFilename`) restricts slug chars to `[a-z0-9-]`. The description has no HTML; YouTube's description field accepts plain text. No injection vector. |
| T-03-10 | Tampering | Title-length overflow | mitigate | Test case 14 asserts realistic combinations fit under YouTube's 100-char title limit. If a future kid name + opponent combo overflows, the upload will be rejected by YouTube; Plan 03-03's error handling surfaces this. No silent truncation at this layer (the truncation policy decision is deferred to Plan 03-03). |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.check.json` exits 0
- `npx vitest run src/publish/templates.test.ts src/publish/errors.test.ts` passes — all 19 template cases + all 8 error cases
- `npx vitest run` passes the full project (Phase 1 + Phase 2 + Plan 03-01 + this plan; no regressions)
- The Node-inline smoke in `<verify>` exits 0 (title + description match expected exact strings character-by-character)
- `prettyOpponent` is imported from `src/render/opponentPretty.ts`, not redefined (grep gates positive + negative both GREEN)
- No new external dependencies (the project already has zod; no new SDK adds)
</verification>

<success_criteria>
Plan complete when:
- PUB-02 (all 9 substitutions: {Kid}, {Opponent}, {scoreFor}, {scoreAgainst}, {result}, {date}, {jersey}, {club}, {source}) is implemented and table-driven tests pass for every substitution
- PUB-03 (opponent slug pretty-print via title-case + hyphen-to-space + `sc`/`fc`/`ac` allow-list) is implemented by REUSING `src/render/opponentPretty.ts`; no duplication
- The 19 test cases (10 table-driven + 9 non-table including character sanity, length sanity, purity, and shape validation) all pass
- TemplateError follows the established `<class>: <field>: <reason>. <remediation>` convention
- The phase is one step closer to shippable; Plans 03-03 + 03-05 can import `renderTemplates` directly
</success_criteria>

<output>
After completion, create `.planning/phases/03-publish-pipeline/03-02-SUMMARY.md` recording:
- Frozen template strings:
  - title: `{Kid} · vs {Opponent} · {scoreFor}–{scoreAgainst} {result} · {date}`
  - description: 5-line LF-separated block per the design spec
- Character pinning: U+00B7 MIDDLE DOT between sections, U+2013 EN DASH between scores. Not ASCII period / hyphen.
- `prettyOpponent` reuse confirmed: imported from `src/render/opponentPretty.ts` (Plan 02-04). No duplication. Per the Plan 02-04 SUMMARY's marker, Phase 3 closed the consolidation question by REUSING in place (no move to src/shared/ was necessary; the import path is clean).
- `TemplateInput` shape — Plan 03-03 + 03-05 construct this from `loadChannel(kid)` + `manifest.game`. Mapping is documented for Plan 03-05's orchestrator.
- TemplateError joins OAuthError as the 2nd error class in `src/publish/errors.ts`. Plans 03-03..03-05 may add more.
- Test seed for table-driven cases — pattern matches Plan 01-02's `channels.test-cases.ts` and Plan 01-03's `filename.test-cases.ts`.
- Title length: case 14 confirms realistic combinations fit under 100 chars. Truncation policy decision deferred to Plan 03-03 (if needed at all — operator-controlled inputs make overflow unlikely for v1).
- Confirmation that Plan 03-01 functionality (OAuth helpers) is unchanged; `src/publish/errors.test.ts` test count grew from 4 → 8.
</output>

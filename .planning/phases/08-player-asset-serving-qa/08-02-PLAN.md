---
phase: "08"
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - web/src/lib/ui/episodeUrl.ts
  - web/src/lib/ui/episodeUrl.test.ts
  - web/src/components/VideoPlayer.tsx
  - web/src/components/VideoPlayer.module.css
  - web/src/components/EpisodeDetail.tsx
  - web/src/components/EpisodeDetail.module.css
  - web/src/app/episodes/[manifestHash]/page.tsx
  - web/tests/detail-player.integration.test.ts
autonomous: true
requirements:
  - PLAY-03
tags:
  - phase-08
  - player-wiring
  - video-element
  - episode-detail
  - server-component
  - client-island

must_haves:
  truths:
    - "Detail page for a rendered episode contains an HTML5 <video> element with controls, preload='metadata', a poster URL matching /api/asset/<kid>/<game>/thumb.png, and src URL matching /api/asset/<kid>/<game>/episode.mp4"
    - "Detail page for a prepared-only episode (no episode.mp4 on disk) does NOT render a <video> element — renders a 'Render the episode to enable playback' hint inside the playerMount section"
    - "Detail page for a published episode renders <video> AND the publish.json YouTube Studio link (existing Phase 7 behavior preserved)"
    - "episodeUrlFor pure helper returns '/api/asset/<encoded-kid>/<encoded-game>/episode.mp4' for any EpisodeIndex-shaped input — mirrors thumbUrlFor"
    - "VideoPlayer is a 'use client' island consuming only string props (src, poster) — no node:* / @golazo/cli imports cross the client boundary"
  artifacts:
    - path: "web/src/lib/ui/episodeUrl.ts"
      provides: "episodeUrlFor pure helper"
      contains: "export function episodeUrlFor"
    - path: "web/src/lib/ui/episodeUrl.test.ts"
      provides: "Unit tests for episodeUrlFor (kid+game encoding, special chars)"
      min_lines: 30
    - path: "web/src/components/VideoPlayer.tsx"
      provides: "Client island wrapping <video controls poster src preload='metadata'>"
      contains: "'use client'"
    - path: "web/src/components/VideoPlayer.module.css"
      provides: "Player styling — full-width container, 16:9 aspect ratio"
    - path: "web/tests/detail-player.integration.test.ts"
      provides: "Integration test asserting <video> tag presence + src + poster URLs on rendered detail page; absence on prepared-only"
      min_lines: 80
  key_links:
    - from: "web/src/components/EpisodeDetail.tsx"
      to: "web/src/components/VideoPlayer.tsx"
      via: "<VideoPlayer src={...} poster={...} />"
      pattern: "VideoPlayer"
    - from: "web/src/components/EpisodeDetail.tsx"
      to: "web/src/lib/ui/episodeUrl.ts"
      via: "import { episodeUrlFor } from '@/lib/ui/episodeUrl'"
      pattern: "episodeUrlFor"
    - from: "web/src/components/EpisodeDetail.tsx"
      to: "web/src/lib/ui/thumbUrl.ts"
      via: "import { thumbUrlFor } from '@/lib/ui/thumbUrl'"
      pattern: "thumbUrlFor"
    - from: "web/src/components/VideoPlayer.tsx"
      to: "browser HTMLVideoElement"
      via: "<video controls preload='metadata' poster={poster} src={src}>"
      pattern: "<video"
---

<objective>
Wire the HTML5 video element into EpisodeDetail's existing `playerMount` seam, fed by a thin client island that consumes only string props. Branch on `row.status` to render the player only for `rendered` / `published` rows; show a hint for `prepared` rows.

Purpose: PLAY-03 — visible player element streams episode.mp4 via the Plan 08-01 route with thumb.png as poster. Depends on 08-01's route landing first (Wave 2).

Output: A new pure `episodeUrlFor` helper (mirrors thumbUrlFor), a minimal `VideoPlayer` client island, updated EpisodeDetail rendering the player conditionally, and an integration test pinning the HTML on the existing detail-view fixture games.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-browse-surface/07-04-SUMMARY.md
@.planning/phases/08-player-asset-serving-qa/08-01-PLAN.md
@web/src/components/EpisodeDetail.tsx
@web/src/lib/ui/thumbUrl.ts
@web/tests/list-view.integration.test.ts

<interfaces>
<!-- thumbUrl pattern to mirror in episodeUrl -->
From web/src/lib/ui/thumbUrl.ts:
```typescript
export function thumbUrlFor(row: Pick<EpisodeIndex, 'kid' | 'gameFolder'>): string;
// Returns: /api/asset/<encodeURIComponent(kid)>/<encodeURIComponent(gameFolder)>/thumb.png
```

<!-- EpisodeIndex shape (subset used here) -->
From web/src/lib/episodeIndex.ts:
```typescript
export interface EpisodeIndex {
  kid: string;
  gameFolder: string;
  status: 'prepared' | 'rendered' | 'published';
  episodeAbsPath: string | null;  // null when no episode.mp4 on disk
  // ... other fields
}
```

<!-- EpisodeDetail current seam (Phase 7 leaves it empty) -->
From web/src/components/EpisodeDetail.tsx:
```typescript
{/* Phase 8 seam: video player will slot in here */}
<section className={styles.playerMount} aria-label="Video player (Phase 8)">
  {/* PLAY-03/04/05: Phase 8 adds <video poster={thumbUrl} src={episodeUrl} controls> here */}
</section>
```

<!-- Detail page existing structure (do NOT break) -->
From web/src/app/episodes/[manifestHash]/page.tsx:
- Calls renderTitle, renderDescription via templates
- Passes { row, manifest, publish, title, description, accent } to <EpisodeDetail>
- Integration test web/tests/detail-view.integration.test.ts (Phase 7) is already green — must remain green
</interfaces>

<!-- Fixture game statuses (web/tests/fixtures/golazo/) -->
- leo/2026-05-13_vs_united_3-1 — manifest only → status 'prepared', no episode.mp4 → NO <video>
- leo/2026-05-20_vs_rivers_2-2 — manifest + episode.mp4 + thumb.png → status 'rendered' → <video> with both src + poster
- mateo/2026-05-27_vs_dragons_4-0 — manifest + episode.mp4 + publish.json + thumb.png → status 'published' → <video> AND YouTube Studio link
Port for this plan's integration test: **4179**
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: episodeUrlFor helper + unit tests + VideoPlayer client island</name>
  <files>web/src/lib/ui/episodeUrl.ts, web/src/lib/ui/episodeUrl.test.ts, web/src/components/VideoPlayer.tsx, web/src/components/VideoPlayer.module.css</files>
  <read_first>
    - web/src/lib/ui/thumbUrl.ts (template: pure, EpisodeIndex-typed Pick, encodeURIComponent on both segments)
    - web/src/lib/ui/thumbUrl.test.ts (test pattern reference if present)
    - web/src/components/EpisodeList.tsx (existing 'use client' island — only one in the project — match its directive placement on line 1 and node:fs-free import policy)
  </read_first>
  <behavior>
    episodeUrlFor:
    - episodeUrlFor({ kid: 'leo', gameFolder: '2026-05-20_vs_rivers_2-2' }) returns '/api/asset/leo/2026-05-20_vs_rivers_2-2/episode.mp4'
    - episodeUrlFor({ kid: 'mateo', gameFolder: 'game with spaces' }) returns '/api/asset/mateo/game%20with%20spaces/episode.mp4'
    - episodeUrlFor({ kid: 'k/id', gameFolder: 'g' }) returns '/api/asset/k%2Fid/g/episode.mp4' (encodes the path-conflicting character — the route handler will reject this via assertSafeAssetPath, but the helper does not pre-validate)
    - Function signature accepts Pick&lt;EpisodeIndex, 'kid' | 'gameFolder'&gt;
    VideoPlayer:
    - Renders `<video controls preload="metadata" poster={poster} src={src} className={styles.video}>` wrapped in a flex container div with className `styles.container`
    - Accepts string props: { src: string; poster: string }
    - First line is `'use client'`
    - No imports from node:*, @golazo/cli, or anything that transitively pulls them
  </behavior>
  <action>
    Create web/src/lib/ui/episodeUrl.ts mirroring thumbUrl.ts exactly — replace the filename literal 'thumb.png' with 'episode.mp4' and export `episodeUrlFor`. JSDoc must mention the Phase 8 route this maps to and call out that this is the ONLY place the URL template lives (single source of truth).

    Write web/src/lib/ui/episodeUrl.test.ts with at minimum 5 cases: basic kid+game, spaces in segment, special chars (slash, percent), empty strings, and a round-trip vs thumbUrlFor confirming both helpers use the same encoding rule.

    Create web/src/components/VideoPlayer.tsx as `'use client'` at line 1. Component signature:
    ```
    interface VideoPlayerProps { src: string; poster: string; }
    export function VideoPlayer({ src, poster }: VideoPlayerProps): React.JSX.Element
    ```
    JSX: outer `<div className={styles.container}>` containing `<video controls preload="metadata" poster={poster} src={src} className={styles.video}>`. NO custom controls, NO event handlers in v2.0 (Phase 8 spec says pure HTML5; browser handles Range natively when Accept-Ranges is advertised by 08-01's route).

    Create web/src/components/VideoPlayer.module.css with `.container` (full-width, max-width matches existing EpisodeDetail content column, margin-block 1rem) and `.video` (width 100%, aspect-ratio 16/9, background black, border-radius small — match overall typographic theme). Hand-mirror any color/spacing tokens from EpisodeDetail.module.css; do NOT import the theme TS module from a CSS module file (Phase 7 decision pinned: CSS Modules cannot import TS tokens — hand-mirror).

    RED commit first: tests present + VideoPlayer stub returning null. GREEN commit second: tests pass + VideoPlayer renders.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run src/lib/ui/episodeUrl.test.ts --reporter=default 2>&amp;1 | tail -15</automated>
    `grep -c "'use client'" web/src/components/VideoPlayer.tsx` MUST equal 1 (first line directive).
    `grep -cE "^import.*from 'node:|from '@golazo/cli" web/src/components/VideoPlayer.tsx` MUST equal 0 (client island purity).
  </verify>
  <done>
    episodeUrlFor exported + 5+ tests pass; VideoPlayer.tsx exists with 'use client' directive + zero forbidden imports; CSS module written with hand-mirrored theme values.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EpisodeDetail wires VideoPlayer into playerMount + page passes URLs + integration test (port 4179)</name>
  <files>web/src/components/EpisodeDetail.tsx, web/src/components/EpisodeDetail.module.css, web/src/app/episodes/[manifestHash]/page.tsx, web/tests/detail-player.integration.test.ts</files>
  <read_first>
    - web/src/components/EpisodeDetail.tsx (current empty playerMount seam — lines around `aria-label="Video player (Phase 8)"`)
    - web/src/components/EpisodeDetail.module.css (existing `.playerMount` class — currently min-height: 0)
    - web/src/app/episodes/[manifestHash]/page.tsx (where EpisodeDetail is constructed — props shape; we will add new props episodeUrl + posterUrl, or compute inside component from row — pick the latter for simpler page-level diff)
    - web/tests/detail-view.integration.test.ts (Phase 7 — existing test on port 4177; this plan ADDS a new test file on 4179 rather than mutating the existing one, so we can keep Phase 7's coverage of Cases 1-6 intact. CRITICAL: scan this file for the literal string `'Video player (Phase 8)'` — if any assertion references that exact aria-label, it MUST be updated in lockstep with the aria-label change in this task; see <action> step "aria-label regression guard")
    - web/src/lib/ui/thumbUrl.ts + new web/src/lib/ui/episodeUrl.ts (URL builders for the new playerMount content)
    - web/src/lib/episodeIndex.ts (row.status discriminator)
  </read_first>
  <behavior>
    EpisodeDetail rendering rules (inside the existing playerMount section):
    - When row.status === 'prepared' (no episode.mp4 on disk): render a `<p className={styles.playerHint}>Render this episode to enable playback.</p>` — no &lt;video&gt; element
    - When row.status === 'rendered' OR row.status === 'published': render &lt;VideoPlayer src={episodeUrlFor(row)} poster={thumbUrlFor(row)} /&gt;
    Detail page integration:
    - All Phase 7 detail integration tests (Cases 1-6 in web/tests/detail-view.integration.test.ts) still pass without modification (aria-label assertion, if any, is updated in lockstep — see action)
    - New integration test (this plan) asserts: rendered fixture HTML contains '&lt;video' AND src='/api/asset/leo/2026-05-20_vs_rivers_2-2/episode.mp4' AND poster='/api/asset/leo/2026-05-20_vs_rivers_2-2/thumb.png'; published fixture HTML contains '&lt;video' AND mateo/dragons episode src; prepared fixture HTML does NOT contain '&lt;video' AND contains 'Render this episode'
  </behavior>
  <action>
    aria-label regression guard (RUN FIRST, before any code change):
    - Run `grep -c "Video player (Phase 8)" web/tests/detail-view.integration.test.ts`. If the count is &gt; 0, the Phase 7 detail integration test asserts the old aria-label literal. In that case, in the SAME COMMIT as the EpisodeDetail aria-label change below, update the assertion in detail-view.integration.test.ts from `'Video player (Phase 8)'` to `'Video player'` (or equivalent regex `/Video player/`). Do NOT split this across commits — the green bar must hold at HEAD on every commit.
    - Also scan with `grep -rn "Video player (Phase 8)" web/` to catch any other test/snapshot referencing the literal. Update every hit in the same commit.
    - If the count is 0, no test update is needed — proceed.

    Modify web/src/components/EpisodeDetail.tsx (Phase 7's existing component):
    - Add imports: `import { VideoPlayer } from './VideoPlayer'; import { thumbUrlFor } from '@/lib/ui/thumbUrl'; import { episodeUrlFor } from '@/lib/ui/episodeUrl';`
    - Inside the existing `<section className={styles.playerMount} aria-label="Video player (Phase 8)">` block, replace the empty comment with a conditional:
      - `row.status === 'prepared'` → `<p className={styles.playerHint}>Render this episode to enable playback.</p>`
      - else → `<VideoPlayer src={episodeUrlFor(row)} poster={thumbUrlFor(row)} />`
    - Update the section's `aria-label` from "(Phase 8)" to just "Video player" (Phase 8 is now landed) — ENSURE the lockstep test update above lands in the SAME commit.
    - DO NOT change the EpisodeDetailProps interface — row already carries kid + gameFolder + status, sufficient inputs for both URL helpers

    Add to web/src/components/EpisodeDetail.module.css:
    - `.playerHint` — muted color, italic, padding-block matching other sections, max-width matching content column

    web/src/app/episodes/[manifestHash]/page.tsx: NO CHANGES needed — props passed to EpisodeDetail already include `row`, and both URL helpers derive everything else from row.kid + row.gameFolder. Confirm by re-reading the page.tsx EpisodeDetail render call and verifying row is in the prop set.

    Write web/tests/detail-player.integration.test.ts on port 4179 mirroring web/tests/detail-view.integration.test.ts structure (spawn, waitForServer, fixture env vars). Cases:
    1. Rendered fixture: GET /episodes/&lt;leo-rivers-manifestHash&gt; → HTML contains `<video` AND attribute pattern matching src='/api/asset/leo/2026-05-20_vs_rivers_2-2/episode.mp4' AND poster URL for the same row. Read the manifestHash dynamically by reading the fixture manifest.json (do NOT hardcode hash).
    2. Published fixture: GET /episodes/&lt;mateo-dragons-manifestHash&gt; → HTML contains `<video` AND src for mateo/dragons/episode.mp4 AND existing YouTube Studio link still present (regression guard for Phase 7 publish section)
    3. Prepared fixture: GET /episodes/&lt;leo-united-manifestHash&gt; → HTML does NOT contain `<video` AND contains 'Render this episode to enable playback' phrase
    4. URL helper agreement: extract the src attribute via regex from case 1's HTML; assert it strictly equals `episodeUrlFor({ kid: 'leo', gameFolder: '2026-05-20_vs_rivers_2-2' })` computed in-test (single-source-of-truth gate — analogous to Phase 7 detail integration's "template equivalence" case)

    Skip gate: `process.env.GOLAZO_SKIP_DETAIL_PLAYER_INTEGRATION === '1'`.

    RED commit first: integration test file present + EpisodeDetail still empty seam (cases 1+2+4 fail; case 3 may pass by accident — still RED gate). GREEN commit: EpisodeDetail wires VideoPlayer; all 4 cases pass; aria-label regression guard update (if needed) lands in this GREEN commit.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vitest run tests/detail-player.integration.test.ts --reporter=default 2>&amp;1 | tail -30</automated>
    All 4 cases pass.
    Then: `cd web &amp;&amp; npx vitest run tests/detail-view.integration.test.ts --reporter=default 2>&amp;1 | tail -15` — Phase 7 detail tests still pass (zero regression).
    Then: `grep -c "VideoPlayer" web/src/components/EpisodeDetail.tsx` MUST be &gt;= 1 (wiring gate).
    Then: `grep -rc "Video player (Phase 8)" web/` MUST equal 0 (aria-label regression guard — no surviving references to the old literal anywhere under web/).
    Then: `cd web &amp;&amp; npx next build 2>&amp;1 | tail -20` exits 0 (no client/server boundary violation — 'use client' contained to VideoPlayer + EpisodeList).
  </verify>
  <done>
    EpisodeDetail conditionally renders VideoPlayer for rendered/published rows; prepared rows show hint; all 4 new integration cases pass; Phase 7 detail integration suite still green (with aria-label assertion updated in lockstep if it referenced the old literal); zero surviving references to "Video player (Phase 8)" under web/; next build clean.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → browser HTML | URL strings (src, poster) embedded in HTML cross to the browser; segments are derived from EpisodeIndex which comes from filesystem scan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Information Disclosure / Tampering | episodeUrlFor + thumbUrlFor URL construction | mitigate | encodeURIComponent on both kid + gameFolder before interpolation — prevents URL injection from filesystem-derived segment names. Final defense is assertSafeAssetPath on the route handler (Plan 08-01); the helpers are not security boundaries but they MUST encode |
| T-08-07 | Information Disclosure | VideoPlayer client island | accept | Receives only two string props; no secrets. The src URL is operator-visible by design (operator's own browser, localhost-only via WEB-02) |
| T-08-08 | Cross-Site Scripting | EpisodeDetail server-rendered HTML | mitigate | React's default JSX escaping covers all interpolated values (src, poster, title, description). No dangerouslySetInnerHTML. The Phase 7 `<pre>{description}</pre>` already established this stance |

</threat_model>

<verification>
- `cd web && npx vitest run src/lib/ui/episodeUrl.test.ts` — 5+ unit cases pass
- `cd web && npx vitest run tests/detail-player.integration.test.ts` — 4 integration cases pass on port 4179
- `cd web && npx vitest run tests/detail-view.integration.test.ts` — Phase 7's 6 cases still pass (regression guard)
- `grep -rc "Video player (Phase 8)" web/` — = 0 (aria-label regression guard; no surviving references to the old literal)
- `cd web && npx tsc --noEmit` — exits 0
- `cd web && npx next build` — exits 0 (no client-boundary node:fs leak)
- `grep -c "'use client'" web/src/components/VideoPlayer.tsx` — = 1
- `grep -c "VideoPlayer" web/src/components/EpisodeDetail.tsx` — >= 1
</verification>

<success_criteria>
1. episodeUrlFor pure helper exists, encodes both segments, mirrors thumbUrlFor signature
2. VideoPlayer client island renders only `<video controls preload="metadata" poster src>` with zero node:* / @golazo/cli imports
3. EpisodeDetail renders VideoPlayer in the existing playerMount section for rendered/published rows; renders 'Render this episode to enable playback' hint for prepared rows
4. New integration test on port 4179 pins the HTML output for all three fixture statuses; URL helper agreement case ties HTML output to episodeUrlFor (single source of truth)
5. Phase 7 detail-view integration tests (Cases 1-6) remain green — zero regression (with aria-label literal assertion updated in the SAME commit as the aria-label change if it existed)
</success_criteria>

<output>
After completion, create `.planning/phases/08-player-asset-serving-qa/08-02-SUMMARY.md` documenting:
- The episodeUrlFor signature and its single-source-of-truth note
- The VideoPlayer client island shape (props, attributes, no event handlers in v2.0)
- The EpisodeDetail status-branch logic with line refs
- Integration test port + skip env var
- Confirmation that page.tsx required zero changes (row already carries inputs)
- Confirmation that Phase 7 detail integration suite still green (including outcome of aria-label regression guard grep: count before / count after / file(s) updated)
- Phase 8 hand-off: Plan 08-04 Playwright golden path will assert play event on the same `<video>` element
</output>

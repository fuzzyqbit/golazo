---
phase: "08"
plan: "04"
type: execute
wave: 4
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
files_modified:
  - web/package.json
  - web/playwright.config.ts
  - web/tests/e2e/golden-path.spec.ts
  - web/tests/e2e/path-traversal.spec.ts
  - web/.gitignore
  - package.json
  - web/README.md
autonomous: true
requirements:
  - WEB-QA-03
tags:
  - phase-08
  - playwright
  - e2e
  - golden-path
  - path-traversal-regression

must_haves:
  truths:
    - "Playwright is installed in web/ as devDependency with chromium-only browser bundle"
    - "`npm run web:e2e` from repo root runs Playwright tests in web/ workspace against a Playwright-managed `npm run dev` server"
    - "Golden path spec opens `/`, applies kid=leo filter, clicks first episode row, asserts video element fires a 'play' event"
    - "Path traversal spec performs `GET /api/asset/..%2F..%2Fetc/passwd-test/episode.mp4` and asserts HTTP 403"
    - "Both specs run against the committed fixture at web/tests/fixtures/golazo/ (env GOLAZO_ROOT + GOLAZO_CHANNELS_PATH set in playwright.config.ts webServer.env)"
  artifacts:
    - path: "web/playwright.config.ts"
      provides: "Playwright config with chromium-only project, webServer block, fixture env injection"
      contains: "webServer"
    - path: "web/tests/e2e/golden-path.spec.ts"
      provides: "Golden path E2E: list → filter → click → play event"
      contains: "video.play"
    - path: "web/tests/e2e/path-traversal.spec.ts"
      provides: "Path traversal regression: ../../etc/passwd attempt → 403"
      contains: "expect(response.status()).toBe(403)"
    - path: "web/package.json"
      provides: "@playwright/test devDep + test:e2e script"
      contains: "@playwright/test"
    - path: "package.json"
      provides: "Root web:e2e script delegating to web workspace"
      contains: "web:e2e"
  key_links:
    - from: "web/playwright.config.ts"
      to: "web/package.json"
      via: "webServer.command = 'npm run dev'"
      pattern: "webServer"
    - from: "web/tests/e2e/golden-path.spec.ts"
      to: "web/src/components/VideoPlayer.tsx"
      via: "page.locator('video').evaluate(v => v.play())"
      pattern: "locator\\('video'"
    - from: "web/tests/e2e/path-traversal.spec.ts"
      to: "web/src/app/api/asset/[kid]/[game]/episode.mp4/route.ts"
      via: "request.get('/api/asset/...')"
      pattern: "/api/asset/"
---

<objective>
Install Playwright with Chromium-only browser bundle in the web/ workspace, configure it with a managed webServer + fixture env injection, and ship two E2E specs: the golden path (list → filter leo → click row → video play event) and the path traversal regression (../../etc/passwd → 403).

Purpose: WEB-QA-03's E2E half. Plan 08-03 ships the coverage half; this plan ships the Playwright half. Combined they close the requirement.

Output: A Chromium-only Playwright install with a single config file, two E2E specs, root + workspace npm scripts, .gitignore additions for Playwright's report/cache dirs, and README docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-player-asset-serving-qa/08-01-PLAN.md
@.planning/phases/08-player-asset-serving-qa/08-02-PLAN.md
@web/tests/list-view.integration.test.ts
@web/src/components/EpisodeList.tsx
@web/src/components/VideoPlayer.tsx

<interfaces>
<!-- Fixture root + channels path env contract (already used by all integration tests) -->
- GOLAZO_ROOT=<abs path to web/tests/fixtures/golazo>
- GOLAZO_CHANNELS_PATH=<abs path to web/tests/fixtures/golazo/channels.yaml>
- HOME=<scratch dir> (token paths are gitignored — match list-view.integration.test.ts pattern with HOME stubbed)

<!-- Fixture status spread (used to pin first row in golden path) -->
- leo/2026-05-13_vs_united_3-1 (prepared — no video)
- leo/2026-05-20_vs_rivers_2-2 (rendered — has video.mp4 + thumb.png)
- mateo/2026-05-27_vs_dragons_4-0 (published — has video.mp4 + thumb.png + publish.json)

<!-- After filter kid=leo with default sort=date.desc -->
First row will be: leo/2026-05-20_vs_rivers_2-2 (newer date than leo/united; only 2 leo games)

<!-- EpisodeRow markup pattern (Phase 7 Plan 03) -->
- Each row is a Link to /episodes/<manifestHash>
- Kid chip has className '.kidChip'
- Per-kid filter chips are buttons / links in EpisodeList client component — confirm exact selector by reading EpisodeList.tsx (kid filter UI element selector pattern)
</interfaces>

<!-- Port selection for Playwright -->
Playwright's webServer runs `npm run dev` which uses port **4173** (web's default). Playwright will wait for `http://127.0.0.1:4173` to respond before starting tests. Set `reuseExistingServer: !process.env.CI` so local re-runs reuse a hot dev server.

<!-- Browser scope decision -->
**Chromium-only** install (devDeps include `@playwright/test` package; browsers installed via `npx playwright install chromium` — NOT all 3). Single-operator workflow on macOS, primary browser is Chrome/Chromium. Firefox + Webkit add ~600MB and roughly double E2E runtime. Documented in SUMMARY; v2.1 backlog if cross-browser regression becomes a real need.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright + chromium-only browser + write playwright.config.ts + gitignore</name>
  <files>web/package.json, web/playwright.config.ts, web/.gitignore</files>
  <read_first>
    - web/package.json (current scripts + devDeps — add new entries without disrupting layout)
    - web/tests/list-view.integration.test.ts (env injection pattern for GOLAZO_ROOT + GOLAZO_CHANNELS_PATH + HOME — webServer.env must mirror this)
    - web/.gitignore if present, else root .gitignore (need to add Playwright artifacts: test-results/, playwright-report/, .playwright/ — match v1.0 coverage/ gitignore convention)
  </read_first>
  <action>
    Step 1: Use Context7 MCP to fetch current Playwright Next.js webServer config docs (`resolve-library-id` "playwright" → `query-docs` for "playwright config webServer Next.js"). Confirm the current recommended `defineConfig` shape — Playwright API evolves on minor versions and our training data may be stale. Pin the exact API used here.

    Step 2: Add @playwright/test to web/package.json devDependencies (latest stable major). Add to scripts:
    - "test:e2e": "playwright test"
    - "test:e2e:install": "playwright install chromium"  (one-shot browser install; not in the main test path)

    Step 3: Run `cd web && npm install` (workspace-aware) then `cd web && npx playwright install chromium` to fetch the browser binary. The browser binary lives in ~/Library/Caches/ms-playwright (macOS) — gitignored by being outside the repo.

    Step 4: Write web/playwright.config.ts using `defineConfig` from `@playwright/test`. Required structure:
    - `testDir: './tests/e2e'` (distinct from existing tests/*.test.ts which is vitest's territory — keep the two test surfaces separate)
    - `fullyParallel: false` (single dev server, sequential simplicity; the operator's Mac is fine on parallelism but the two specs are short and we avoid worker contention on the fixture)
    - `forbidOnly: !!process.env.CI`
    - `retries: process.env.CI ? 2 : 0`
    - `workers: 1` (single shared webServer)
    - `reporter: process.env.CI ? 'github' : 'list'`
    - `use`: `{ baseURL: 'http://127.0.0.1:4173', trace: 'on-first-retry', viewport: { width: 1440, height: 900 } }` (desktop viewport — UI-06 declares desktop-first)
    - `projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }]` — single chromium project
    - `webServer`:
      - `command: 'npm run dev'`  (uses web's existing `dev` script which binds 127.0.0.1:4173)
      - `cwd` defaults to web/ when config is in web/ — confirm via Context7 docs from Step 1
      - `url: 'http://127.0.0.1:4173'`
      - `timeout: 120_000`
      - `reuseExistingServer: !process.env.CI`
      - `env`:
        - `GOLAZO_ROOT`: absolute path to web/tests/fixtures/golazo via `resolve(__dirname, 'tests/fixtures/golazo')` — compute at config load
        - `GOLAZO_CHANNELS_PATH`: `<GOLAZO_ROOT>/channels.yaml`
        - `HOME`: `process.env.HOME` passthrough (Playwright's webServer doesn't inherit by default for env keys it sets — pin explicitly so token-path tilde resolution works)
        - `NODE_ENV`: 'test'

    Step 5: Add to web/.gitignore (create if missing):
    - `/test-results/`
    - `/playwright-report/`
    - `/playwright/.cache/`
    - `/.playwright/`
    - Existing entries preserved.

    DO NOT install Firefox or Webkit. DO NOT enable trace/video on every run (only on-first-retry to keep dev runs fast).
  </action>
  <verify>
    <automated>cd web &amp;&amp; test -f playwright.config.ts &amp;&amp; npx playwright --version &amp;&amp; grep -c "@playwright/test" web/package.json &amp;&amp; grep -c "webServer" web/playwright.config.ts &amp;&amp; grep -c "chromium" web/playwright.config.ts</automated>
    All greps return ≥ 1; playwright --version prints the installed version.
    `grep -c "firefox\|webkit" web/playwright.config.ts` MUST equal 0 (Chromium-only gate).
    `grep -c "test-results" web/.gitignore` MUST equal 1.
  </verify>
  <done>
    @playwright/test installed in web workspace; Chromium browser fetched; web/playwright.config.ts written with chromium-only project + managed webServer + fixture env injection; .gitignore entries added; no Firefox/Webkit installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Golden path E2E spec + path-traversal regression spec + root scripts + README</name>
  <files>web/tests/e2e/golden-path.spec.ts, web/tests/e2e/path-traversal.spec.ts, package.json, web/README.md</files>
  <read_first>
    - web/playwright.config.ts (from Task 1 — for testDir and baseURL)
    - web/src/components/EpisodeList.tsx (kid filter UI element — extract exact selectors: are filter chips `<button>` or `<a>`? What class names? What ARIA roles? Read the JSX and pick the most stable selector — prefer role + accessible name, e.g. `getByRole('link', { name: /leo/i })`)
    - web/src/components/EpisodeRow.tsx (row container element — pick selector: prefer `getByRole('link')` filtered by accessible name or position; first-row selection via `first()`)
    - web/src/components/VideoPlayer.tsx (from Plan 08-02 — confirms `<video>` element is the locator target; no custom controls)
    - web/src/lib/ui/episodeUrl.ts (from Plan 08-02 — for the URL pattern the traversal regression mimics)
    - web/tests/fixtures/golazo/leo/2026-05-20_vs_rivers_2-2/.golazo/episode.mp4 — the fixture mp4 the golden path will attempt to play. ASSUMPTION: this is a decodable MP4 (even a 1-second silent black-frame ISO BMFF clip is fine). If the committed file is a byte-only stub or otherwise not decodable by Chromium's media stack, the `currentTime > 0` assertion in Step 11 below cannot pass — in that case Step 11 MUST be skipped (see &lt;action&gt; for the conditional skip protocol). Determine decodability at &lt;read_first&gt; time by inspecting the first 12 bytes for an `ftyp` box (`hexdump -C <file> | head -1` — bytes 4..7 should read `66 74 79 70`) and capture the result for the spec.
  </read_first>
  <action>
    Use Context7 docs (resolved in Task 1) for the canonical Playwright assertion APIs being used here (page.goto, locator role queries, video element evaluate, request.get, expect status).

    Fixture decodability gate (RUN FIRST — determines Step 11 behavior):
    - Run `hexdump -C web/tests/fixtures/golazo/leo/2026-05-20_vs_rivers_2-2/.golazo/episode.mp4 | head -1` and confirm bytes 4..7 are `66 74 79 70` (`ftyp`). If yes, the fixture is a valid ISO BMFF MP4 and Step 11 (currentTime > 0) is enabled. If no (file is a byte-only stub, e.g. all zeros or random bytes), the fixture is NOT decodable by Chromium's media stack — Step 11 MUST be commented out in the spec and the `play` event firing (Step 10) is the strongest assertion available. Record the decision in &lt;done&gt; and in the SUMMARY.

    Spec 1: web/tests/e2e/golden-path.spec.ts
    - `import { test, expect } from '@playwright/test'`
    - `test('golden path: list → filter leo → click first → video plays', async ({ page }) => {`
      1. `await page.goto('/')` — list view loads
      2. Assert at least 3 rows visible: `await expect(page.locator('a[href^="/episodes/"]')).toHaveCount.greaterThanOrEqualTo(3)` (3 fixture games, ignoring any broken-folder)
      3. Click the leo filter chip — selector resolved by reading EpisodeList.tsx in &lt;read_first&gt;; preferred: `await page.getByRole('link', { name: 'leo' }).click()` or `getByRole('button', {...})` based on actual markup. URL should change to `/?kid=leo`: `await expect(page).toHaveURL(/kid=leo/)`
      4. After filter, only 2 leo rows remain: `await expect(page.locator('a[href^="/episodes/"]')).toHaveCount(2)`
      5. Click first row: `await page.locator('a[href^="/episodes/"]').first().click()`
      6. Wait for detail page: `await page.waitForURL(/\/episodes\//)`
      7. Locate the video element from Plan 08-02's VideoPlayer: `const video = page.locator('video')`
      8. Assert it has the expected src + poster attributes: `await expect(video).toHaveAttribute('src', /\/api\/asset\/leo\/2026-05-20_vs_rivers_2-2\/episode\.mp4/)` and `toHaveAttribute('poster', /thumb\.png/)`
      9. Wait for metadata to load (preload="metadata" advertises Range support — the request will hit our route): `await video.evaluate((v: HTMLVideoElement) => new Promise((resolve, reject) => { v.addEventListener('loadedmetadata', () => resolve(undefined), { once: true }); v.addEventListener('error', () => reject(new Error('video error')), { once: true }); setTimeout(() => reject(new Error('loadedmetadata timeout')), 15_000); }))`
         NOTE: if the fixture is a byte-only stub (decodability gate above returned no), `loadedmetadata` will never fire — in that branch, replace Step 9 with `await page.waitForTimeout(500);` and skip both Steps 9 + 11; assert only that the `<video>` element exists with correct src/poster (Steps 7+8) and that the `play` event fires (Step 10). The structural assertions PLUS the `play` event are sufficient for WEB-QA-03 if the fixture is not decodable.
      10. Trigger play and assert the play event fires: `await video.evaluate((v: HTMLVideoElement) => new Promise((resolve, reject) => { v.addEventListener('play', () => resolve(undefined), { once: true }); v.play().catch(reject); setTimeout(() => reject(new Error('play timeout')), 10_000); }))`
         NOTE: `play` fires when `play()` is invoked, regardless of whether decoding actually progresses — this assertion holds for both decodable and stub fixtures and is the primary gate.
      11. OPTIONAL (only if decodability gate returned YES): assert currentTime increments after 500ms. The spec file MUST gate this block behind a clearly labeled comment so a future reader knows it depends on the fixture being decodable:
         ```
         // optional: skip if fixture is not a decodable MP4 — `play` event firing (Step 10) is sufficient
         // await page.waitForTimeout(500);
         // const t = await video.evaluate((v: HTMLVideoElement) => v.currentTime);
         // expect(t).toBeGreaterThan(0);
         ```
         If the decodability gate returned YES, UN-comment the block and keep the explanatory comment above it. If the gate returned NO, leave the block commented out (the comment is part of the spec file's permanent documentation explaining why the strongest assertion is `play` rather than `currentTime > 0`).

    Spec 2: web/tests/e2e/path-traversal.spec.ts
    - `import { test, expect } from '@playwright/test'`
    - `test('path traversal: ../../etc/passwd attempt returns 403', async ({ request }) => {`
      1. `const response = await request.get('/api/asset/' + encodeURIComponent('..') + '/' + encodeURIComponent('etc') + '/episode.mp4')`
      2. `expect(response.status()).toBe(403)` — pins Plan 08-01's assertSafeAssetPath rejection path through the live route handler
      3. Second case: assert the thumb route (Phase 7) also rejects: `const thumbResponse = await request.get('/api/asset/' + encodeURIComponent('..') + '/' + encodeURIComponent('etc') + '/thumb.png'); expect(thumbResponse.status()).toBe(403);` — pins WEB-QA-03's "request /api/asset/../../etc/passwd returns 403" criterion across both asset routes
      4. Third case: literal traversal in path-encoded form (the requirement text says `GET /api/asset/../../etc/passwd`). Some HTTP clients normalize `..` before sending; Playwright's request fixture sends the literal path. Test: `const literalResponse = await request.get('/api/asset/..%2F..%2Fetc%2Fpasswd/episode.mp4'); expect(literalResponse.status()).toBe(403);` — covers URL-encoded `..` traversal

    Step 3: Add to root package.json scripts (after web:coverage from Plan 08-03):
    - "web:e2e": "npm run test:e2e --workspace=@golazo/web"
    - "web:e2e:install": "npm run test:e2e:install --workspace=@golazo/web"
    Preserve all other scripts.

    Step 4: Extend web/README.md `## Testing` section (Plan 08-03's edit) with an `### E2E (Playwright)` subsection documenting:
    - First-time setup: `npm run web:e2e:install` (downloads Chromium ~150MB)
    - Run E2E: `npm run web:e2e` (Playwright manages dev server lifecycle via webServer config)
    - Specs covered: golden path (list → filter → click → play), path traversal regression (3 sub-cases)
    - Browser scope: chromium-only — operator's Mac primary browser is Chrome/Chromium; Firefox + Webkit deferred to v2.1
    - Visual regression note: pixelmatch-style visual snapshots deferred to v2.1 (golden-path structural assertions are sufficient for v2.0)
    - Fixture decodability note: the golden-path spec's `play` event assertion holds regardless of whether `episode.mp4` is a decodable MP4 or a byte-only stub; the optional `currentTime > 0` assertion is enabled only when the fixture begins with an `ftyp` box (decodable ISO BMFF). Operators regenerating the fixture should keep this property in mind.

    DO NOT pre-run Playwright in CI in this plan — wiring CI is out of scope for v2.0 (no CI exists yet per project context). The script lives at root for manual operator use.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx playwright test --reporter=list 2>&amp;1 | tail -25</automated>
    Both specs pass:
    - golden-path.spec.ts → "golden path: list → filter leo → click first → video plays" PASSED
    - path-traversal.spec.ts → "path traversal: ../../etc/passwd attempt returns 403" PASSED (3 expect calls inside)
    Then: `grep -c "web:e2e" package.json` MUST equal &gt;= 1.
    Then: `grep -c "Playwright\|playwright" web/README.md` MUST be &gt;= 1.
    Then: `grep -c "optional: skip if fixture is not a decodable MP4" web/tests/e2e/golden-path.spec.ts` MUST be &gt;= 1 (documents Step 11's fixture-dependence regardless of whether it's enabled or commented).
  </verify>
  <done>
    Two E2E specs exist under web/tests/e2e/; `npm run web:e2e` from repo root passes both specs against the committed fixture; root script delegates to web workspace; README documents setup + run + Chromium-only scope + visual regression deferral + fixture decodability note; golden-path.spec.ts contains the explanatory comment for the optional Step 11 (currentTime > 0) so the fixture-dependence is permanently documented. Fixture decodability decision recorded: {YES, Step 11 enabled} or {NO, Step 11 commented out — `play` event is the strongest assertion}.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright managed dev server → ~/golazo fixture | E2E spawns next dev pointing at web/tests/fixtures/golazo via GOLAZO_ROOT — same boundary as integration tests; localhost-only (WEB-02 enforced inside the spawned dev server) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Tampering | Playwright path traversal spec | mitigate (verify) | The spec itself is a test that EXERCISES the path-safety mitigation (Plan 08-01's assertSafeAssetPath via the route handler) — confirming end-to-end that traversal returns 403. The mitigation lives in Plan 08-01; this plan pins it from outside the process boundary |
| T-08-11 | Information Disclosure | Playwright trace files | mitigate | `trace: 'on-first-retry'` — traces only generated on retry, never committed; test-results/ and playwright-report/ added to .gitignore. Traces may contain fixture data which is committed test data anyway (no real PII in the fixture) |
| T-08-12 | DoS | webServer reuseExistingServer | accept | Local dev only; operator's Mac. CI lane re-spawns fresh server per run. No multi-user shared infrastructure |

</threat_model>

<verification>
- `cd web && npx playwright test` exits 0 with 2 specs / 2 tests passing
- `npm run web:e2e` from repo root passes same suite
- `grep -c "firefox\|webkit" web/playwright.config.ts` = 0 (Chromium-only gate)
- `grep -c "webServer" web/playwright.config.ts` >= 1 (managed server gate)
- `grep -c "GOLAZO_ROOT" web/playwright.config.ts` >= 1 (fixture env injection gate)
- `web/.gitignore` contains `test-results/`, `playwright-report/`, `playwright/.cache/`
- `web/README.md` has E2E subsection documenting setup, run, browser scope, visual-regression deferral, fixture decodability note
- `grep -c "optional: skip if fixture is not a decodable MP4" web/tests/e2e/golden-path.spec.ts` >= 1 (Step 11 fixture-dependence permanently documented in the spec file)
- Phase 7's existing vitest integration tests STILL pass (no regression to ports 4175/4177; Playwright uses 4173 — disjoint)
</verification>

<success_criteria>
1. @playwright/test installed in web workspace; chromium-only browser binary present (~/Library/Caches/ms-playwright)
2. web/playwright.config.ts written: testDir './tests/e2e', baseURL 127.0.0.1:4173, single chromium project, webServer with GOLAZO_ROOT + GOLAZO_CHANNELS_PATH env injection
3. golden-path.spec.ts: list → filter leo → click first row → assert <video> element + src/poster attributes + `play` event fires. Optional Step 11 (currentTime > 0) is enabled only when the fixture mp4 begins with an `ftyp` box (decodable ISO BMFF); when not enabled, the block remains in the spec as a documented commented-out branch with explanatory comment
4. path-traversal.spec.ts: 3 sub-cases pinning 403 on both episode.mp4 + thumb.png routes via encoded traversal
5. Root `npm run web:e2e` delegates to web workspace; one command from repo root runs the whole suite
6. README documents setup, run, Chromium-only scope, visual-regression deferral, fixture decodability note
7. Visual regression formally deferred to v2.1 (recommendation accepted)
</success_criteria>

<output>
After completion, create `.planning/phases/08-player-asset-serving-qa/08-04-SUMMARY.md` documenting:
- Final playwright.config.ts shape (paste full text)
- Browser scope decision pinned: chromium-only (Firefox + Webkit deferred to v2.1)
- Visual regression decision pinned: DEFERRED to v2.1 (rationale: golden-path structural assertions adequate for single-operator v2.0; pixelmatch + headless rendering adds nondeterminism without proportional value)
- Fixture decodability decision pinned: YES (Step 11 enabled) or NO (Step 11 commented out — `play` event is the strongest assertion); include the hexdump result that drove the decision
- golden-path.spec.ts step list + per-step assertion (mark Step 11 enabled/commented based on fixture decodability)
- path-traversal.spec.ts 3 sub-case table (URL × expected status × which route handler)
- Phase 8 final hand-off: all 6 phase requirements (PLAY-03..05, WEB-QA-01..03) closed across Plans 08-01..04; v2.0 milestone ready for verification
- Any deviations + rationale
</output>

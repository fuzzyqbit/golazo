---
phase: 02-render-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - remotion/assets/fonts/CormorantGaramond-Italic.ttf
  - remotion/assets/fonts/Inter-Regular.ttf
  - remotion/assets/fonts/Inter-Bold.ttf
  - remotion/assets/fonts/README.md
  - remotion/theme/tokens.ts
  - remotion/theme/fonts.ts
  - remotion/theme/grade.ts
  - remotion/theme/index.ts
  - remotion/theme/tokens.test.ts
  - remotion/theme/grade.test.ts
  - .gitignore
  - tsconfig.json
  - tsconfig.check.json
  - package.json
autonomous: true
requirements:
  - REN-03

must_haves:
  truths:
    - "Importing `remotion/theme` resolves the named exports `COLORS`, `TYPOGRAPHY`, `LAYOUT`, `MOTION`, `loadFonts`, `getCinematicGradeStyle`, `getVignetteOverlayStyle` without runtime error"
    - "`remotion/assets/fonts/` contains Cormorant Garamond Italic .ttf and Inter Regular + Bold .ttf files committed to git (each > 10kB), license note in README.md"
    - "`loadFonts()` returns the resolved family names `Cormorant Garamond` and `Inter` after registering both fonts via `@remotion/fonts` (or equivalent Remotion font registration API)"
    - "`getCinematicGradeStyle()` returns a CSS-properties object containing the documented `filter` (saturate + contrast + brightness tweak) and `getVignetteOverlayStyle()` returns a radial-gradient overlay style"
    - "Theme tokens are pure constants — no I/O, no React imports, no Remotion runtime APIs at module-load time (so the file is importable by the render driver too)"
    - "Operator running `npm run typecheck` (alias `tsc --noEmit -p tsconfig.check.json`) succeeds with the new `remotion/` tree included"
    - "Operator running `npm run build` (alias `tsc -p tsconfig.json`) still emits `dist/cli/index.js` at the path referenced by `package.json#bin` — Phase 1 CLI bin contract preserved"
  artifacts:
    - path: "remotion/theme/tokens.ts"
      provides: "COLORS, TYPOGRAPHY, LAYOUT, MOTION named constants — pure data, no imports beyond TS types"
      exports: ["COLORS", "TYPOGRAPHY", "LAYOUT", "MOTION"]
    - path: "remotion/theme/fonts.ts"
      provides: "loadFonts() — registers Cormorant Garamond Italic + Inter Regular/Bold via @remotion/fonts; returns the family names compositions can pass to fontFamily"
      exports: ["loadFonts", "FONT_FAMILIES"]
    - path: "remotion/theme/grade.ts"
      provides: "getCinematicGradeStyle() + getVignetteOverlayStyle() — return inline-style objects for CSS filter + radial-gradient overlay"
      exports: ["getCinematicGradeStyle", "getVignetteOverlayStyle"]
    - path: "remotion/theme/index.ts"
      provides: "Barrel re-exporting tokens + fonts + grade — single import point for compositions and the render driver"
      exports: ["*"]
    - path: "remotion/assets/fonts/"
      provides: "Self-hosted TTF files: Cormorant Garamond Italic, Inter Regular, Inter Bold + LICENSE/README.md noting SIL OFL provenance"
      contains: "CormorantGaramond-Italic.ttf"
    - path: "tsconfig.check.json"
      provides: "Type-check-only config that extends tsconfig.json and adds remotion/**/* to include. Used by `npm run typecheck` (--noEmit). The base tsconfig.json stays unchanged for the emit-to-dist build that backs package.json#bin."
  key_links:
    - from: "remotion/theme/fonts.ts"
      to: "remotion/assets/fonts/"
      via: "import font URLs via staticFile or relative path"
      pattern: "CormorantGaramond-Italic|Inter-Regular|Inter-Bold"
    - from: "remotion/theme/index.ts"
      to: "remotion/theme/{tokens,fonts,grade}.ts"
      via: "barrel re-exports"
      pattern: "export \\* from"
    - from: "tsconfig.check.json"
      to: "tsconfig.json"
      via: "extends"
      pattern: "\"extends\".*tsconfig\\.json"
---

<objective>
Stand up the visual-theme primitives the Phase 2 Remotion compositions and render driver will compose on top of: self-hosted display + label fonts, color/typography/layout/motion token constants, and the cinematic-grade + vignette CSS helpers (REN-03).

Purpose: Each downstream plan in Phase 2 (Plans 02, 03, 04) will import from `remotion/theme`. Isolating theme primitives into a leaf plan with zero dependencies on the manifest schema or on Remotion's composition runtime lets Plan 02 (music picker — pure logic) and this plan run in parallel in Wave 1, and gives Plan 03 (compositions) a stable surface to consume.

Output: `remotion/assets/fonts/` with three committed `.ttf` files (Cormorant Garamond Italic + Inter Regular + Inter Bold), `remotion/theme/` module exporting tokens + `loadFonts()` + grade/vignette helpers, and a `tsconfig.check.json` that extends the base config to also type-check the `remotion/` tree under `--noEmit`. The base `tsconfig.json`'s `rootDir: "./src"` and `outDir: "./dist"` stay unchanged so Phase 1's `package.json#bin` -> `./dist/cli/index.js` keeps resolving exactly as before. Plan 03 consumes the theme surface directly; Plan 02 (music picker) has no theme dependency and runs alongside this in Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/superpowers/specs/2026-05-13-golazo-design.md
@.planning/phases/01-foundation-prepare-pipeline/01-05-SUMMARY.md

<interfaces>
<!-- Contracts produced by this plan, consumed by Plans 02-04. -->
<!-- NodeNext .js extensions for cross-module imports (tsconfig already enforces this). -->

remotion/theme/tokens.ts (pure data — no runtime imports):

  Concrete constants (numerical defaults pinned here so Plan 03's compositions
  do not need to negotiate values — change values here, every composition
  picks them up):

  export const COLORS = {
    background: '#0a0a0a',          // near-black backdrop
    foreground: '#f5f1e8',          // warm-cream typography colour
    accentDefault: '#ffce5a',       // overridden per kid via ChannelConfig.accent
    chapterCardBg: '#0e0e0e',
    vignetteEdge: 'rgba(0,0,0,0.6)',
  } as const;

  export const TYPOGRAPHY = {
    display: { family: 'Cormorant Garamond', weight: 400, style: 'italic' as const },
    label:   { family: 'Inter',              weight: 400, style: 'normal' as const },
    labelBold: { family: 'Inter',            weight: 700, style: 'normal' as const },
    sizes: { hero: 96, sub: 36, chapter: 64, score: 144, caption: 18 }, // px @ 1920x1080
    letterSpacing: { display: 0.02, label: 0.16 }, // em
  } as const;

  export const LAYOUT = {
    width: 1920,
    height: 1080,
    safeMargin: 96,
    cardPaddingX: 160,
  } as const;

  export const MOTION = {
    titleCardFrames: 90,    // 3s @ 30fps
    chapterCardFrames: 45,  // 1.5s @ 30fps
    outroFrames: 90,        // 3s @ 30fps
    crossfadeFrames: 12,    // ~0.4s
    fps: 30,
    firstClipPlaybackRate: 0.5,  // slo-mo opener — see REN-01
  } as const;

remotion/theme/fonts.ts:

  Use `@remotion/fonts` (Remotion 4.x official font-loading package). Register
  three faces from the staticFile-resolved URLs of the committed `.ttf` files.

  export const FONT_FAMILIES = {
    display: 'Cormorant Garamond',
    label: 'Inter',
  } as const;

  export async function loadFonts(): Promise<{ display: string; label: string }>;
  // Calls loadFont/loadFonts from '@remotion/fonts' for each face; returns
  // the resolved family names. Idempotent — repeated calls are no-ops at
  // the browser/CSS layer because the same family+weight is requested.

remotion/theme/grade.ts (returns inline-style objects, NOT JSX — keeps file
React-free so the render driver can introspect without bundling):

  export function getCinematicGradeStyle(): React.CSSProperties;
  // Returns: { filter: 'saturate(1.12) contrast(1.05) brightness(0.96)' }
  // Plan 03 spreads this into the Clip + Episode root layer's style.

  export function getVignetteOverlayStyle(): React.CSSProperties;
  // Returns: {
  //   position: 'absolute', inset: 0, pointerEvents: 'none',
  //   background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.6) 100%)',
  // }
  // Plan 03 renders an absolute-positioned div with this style on top of
  // every Clip/Card layer.

remotion/theme/index.ts:
  Barrel: re-export everything from tokens.ts, fonts.ts, grade.ts.

tsconfig.check.json (NEW — type-check-only config; extends the base tsconfig.json
without changing its rootDir or outDir):

  {
    "extends": "./tsconfig.json",
    "compilerOptions": {
      "noEmit": true,
      "rootDir": "."
    },
    "include": ["src/**/*", "remotion/**/*"]
  }

  Rationale: the base tsconfig.json keeps `rootDir: "./src"` and `outDir: "./dist"`
  so that `npm run build` (`tsc -p tsconfig.json`) still emits `dist/cli/index.js`
  at the exact path Phase 1's `package.json#bin.golazo` ("./dist/cli/index.js")
  references. The check-config widens rootDir to the project root and adds
  `remotion/**/*` to include — but only for `--noEmit` type-checking, so no
  files get emitted under `dist/src/...` or `dist/remotion/...`. Remotion's
  own bundler (invoked by `npx remotion bundle` and by `@remotion/bundler`'s
  `bundle()` call in Plan 02-04) compiles the TSX in `remotion/` directly via
  its own webpack pipeline — it does NOT consume `tsc`'s emit. So we never
  need `tsc` to emit anything under `remotion/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit self-hosted fonts + add tsconfig.check.json (type-check-only) + extend .gitignore — base tsconfig.json + package.json#bin UNCHANGED</name>
  <files>remotion/assets/fonts/CormorantGaramond-Italic.ttf, remotion/assets/fonts/Inter-Regular.ttf, remotion/assets/fonts/Inter-Bold.ttf, remotion/assets/fonts/README.md, .gitignore, tsconfig.check.json, package.json</files>
  <read_first>
    - docs/superpowers/specs/2026-05-13-golazo-design.md (sections: Architecture > top-level project layout; Open Items > display fonts)
    - .planning/REQUIREMENTS.md (REN-03)
    - tsconfig.json (current rootDir + include + exclude — read but do NOT modify in this task; verify the existing `rootDir: "./src"` and `include: ["src/**/*"]`; the new `tsconfig.check.json` extends this WITHOUT mutating it)
    - package.json (current `bin.golazo` value = "./dist/cli/index.js", and `scripts.typecheck` = "tsc --noEmit" — this task updates `scripts.typecheck` to point at the new check-config; `bin` is NOT touched)
    - .planning/phases/01-foundation-prepare-pipeline/01-01-PLAN.md (Phase 1's contract: `bin.golazo` = `./dist/cli/index.js`; `tsc -p .` produces that path; this plan must NOT break either)
    - .gitignore (existing exception list — add the font + assets binary exceptions following the same idiom as `!tests/fixtures/...`)
    - Context7 `@remotion/fonts` docs (resolve-library-id with `remotion fonts` then query-docs for the `loadFont` / `staticFile` integration in 4.x) — note for Task 2's font URL pattern
    - Context7 Remotion 4.x bundler docs (resolve-library-id with `remotion bundler` then query-docs for "entryPoint TSX compilation") — CONFIRMS that `bundle({ entryPoint: 'remotion/Root.tsx' })` compiles TSX directly via webpack; `tsc` emit is NOT in the Remotion bundle pipeline. This is the load-bearing fact justifying why we do NOT need `tsc` to emit `remotion/**/*`.
  </read_first>
  <behavior>
    No unit tests in this task — it commits binary assets and edits config files. Verification is structural:
    - All three TTF files exist under `remotion/assets/fonts/` and are non-empty (>10kB each).
    - `tsconfig.check.json` exists at the project root, extends `./tsconfig.json`, sets `noEmit: true` + `rootDir: "."`, and includes both `src/**/*` and `remotion/**/*`.
    - `tsconfig.json` is UNTOUCHED (still `rootDir: "./src"`, `outDir: "./dist"`, `include: ["src/**/*"]`).
    - `package.json` scripts.typecheck is updated to `tsc --noEmit -p tsconfig.check.json` (so `npm run typecheck` covers BOTH `src/` and `remotion/`); `package.json` scripts.build and `package.json#bin` are UNTOUCHED.
    - `.gitignore` whitelists the committed TTF files so they survive `git add` regardless of any broad `*.ttf` rule (if one exists; if not, add an explicit allow comment to anchor the pattern for future readers).
    - The `remotion/assets/fonts/README.md` declares the license (SIL OFL 1.1 for both Cormorant Garamond and Inter — both fonts ship under SIL OFL, which permits embedding/redistribution with attribution) and the source URLs the operator used to fetch them.
    - Phase 1 bin path still resolves AFTER this task: `node -e "require.resolve('./' + require('./package.json').bin.golazo)"` exits 0 once `dist/cli/index.js` has been produced by `npm run build`. (Since this task does not delete `dist/`, the existing build artifact from Phase 1 — if present — still resolves; if `dist/` is gitignored and absent, running `npm run build` is part of the verify step.)
  </behavior>
  <action>
    Download Cormorant Garamond Italic (SIL OFL 1.1) from a Google Fonts CDN or fontsource — concrete sources to try in order: (1) `https://fonts.gstatic.com/s/cormorantgaramond/v18/...Italic.ttf`, (2) the `@fontsource/cormorant-garamond` package's `files/cormorant-garamond-latin-400-italic.ttf` bundled under `node_modules` if it is installed, (3) `https://github.com/CatharsisFonts/Cormorant/raw/master/2_Variable_TTF/CormorantGaramond-Italic.ttf`. Save as `remotion/assets/fonts/CormorantGaramond-Italic.ttf`. Use `curl -L --fail` and verify the downloaded file is `>10kB` and `file <path>` reports `TrueType Font`.

    Download Inter Regular + Inter Bold (SIL OFL 1.1) from Google Fonts CDN or fontsource similarly. Save as `remotion/assets/fonts/Inter-Regular.ttf` and `remotion/assets/fonts/Inter-Bold.ttf`. Same `file`-format and size checks.

    If none of the CDN sources succeed (offline / network-restricted machine), fall back to: `npm install --save-dev @fontsource/cormorant-garamond @fontsource/inter` and copy the `.ttf` files out of `node_modules/@fontsource/<pkg>/files/` into `remotion/assets/fonts/`. Record the chosen source in the README.

    Create `remotion/assets/fonts/README.md` with:
    - Short bullet list of the three font files
    - License declaration: "Both fonts ship under the SIL Open Font License 1.1. See https://openfontlicense.org/ for the full text. Source: <recorded source URLs from the download step above>."
    - Note: "Self-hosted (not loaded from Google Fonts CDN at render-time) so renders are deterministic and offline-safe — Remotion would otherwise hit the network during bundle, breaking reproducibility."

    Create `tsconfig.check.json` at the project root (verbatim content; copy this exactly):

    ```
    {
      "extends": "./tsconfig.json",
      "compilerOptions": {
        "noEmit": true,
        "rootDir": "."
      },
      "include": ["src/**/*", "remotion/**/*"]
    }
    ```

    Rationale embedded in this plan's `<interfaces>` block and in the Phase-2 SUMMARY: the base `tsconfig.json` keeps emitting `dist/cli/index.js` at the path Phase 1 promised; the check-config widens `rootDir` to `.` and adds `remotion/**/*` to include for type-check coverage ONLY. We do NOT touch `tsconfig.json` — DO NOT add `remotion/**/*` to its `include`, DO NOT change its `rootDir`, DO NOT change its `outDir`. Confirm by `diff`-ing the file before and after this task: the only `tsconfig.json` change permitted is NONE.

    Edit `package.json` `scripts.typecheck` from `"tsc --noEmit"` to `"tsc --noEmit -p tsconfig.check.json"`. Do NOT modify `scripts.build`, `bin`, `dependencies`, `devDependencies`, `engines`, or any other top-level field. Run `npm run typecheck` to confirm zero errors across `src/` (existing Phase 1 code; should be no-op) — the check is unconditional here because no remotion/ files exist yet, so the new include line is a no-op at this point. Confirm `npm run build` still produces `dist/cli/index.js` and `node -e "require.resolve('./' + require('./package.json').bin.golazo)"` exits 0.

    Edit `.gitignore` (ADD BOTH BLOCKS HERE so Plan 02-02 — running in parallel in Wave 1 — does not need to touch `.gitignore` and the two plans stay file-disjoint):
    - Below the existing `# Allow committed integration-fixture binaries` block, add an analogous `# Allow committed Remotion font assets` block with the explicit allow patterns:
      ```
      # Allow committed Remotion font assets — see remotion/assets/fonts/README.md
      !remotion/assets/fonts/*.ttf

      # Allow committed Remotion music fixtures — see remotion/assets/music/README.md
      # (Plan 02-02 commits the .mp3 pool + index.json under remotion/assets/music/)
      !remotion/assets/music/*.mp3
      !remotion/assets/music/index.json
      ```
    - If no broad rule (`*.ttf` / `*.mp3`) exists yet, the allow lines are informational but still anchor the intent for future readers.

    Verify by running `git check-ignore -v remotion/assets/fonts/CormorantGaramond-Italic.ttf` AND `git check-ignore -v remotion/assets/music/atmos-1.mp3` — both must report NOT ignored (no rule matching) OR report a `!` allow rule winning. If git reports either path is ignored, refine the exclusion until it is not. (The .mp3 file does not exist yet — that's Plan 02-02's task; the check verifies the RULE permits the path, not the file's presence.)
  </action>
  <verify>
    <automated>cd /Users/me/Documents/code/golazo && test -s remotion/assets/fonts/CormorantGaramond-Italic.ttf && test -s remotion/assets/fonts/Inter-Regular.ttf && test -s remotion/assets/fonts/Inter-Bold.ttf && test "$(wc -c < remotion/assets/fonts/CormorantGaramond-Italic.ttf | tr -d ' ')" -gt 10000 && test "$(wc -c < remotion/assets/fonts/Inter-Regular.ttf | tr -d ' ')" -gt 10000 && test "$(wc -c < remotion/assets/fonts/Inter-Bold.ttf | tr -d ' ')" -gt 10000 && file remotion/assets/fonts/CormorantGaramond-Italic.ttf | grep -qi 'TrueType' && test -f tsconfig.check.json && grep -q '"extends": "./tsconfig.json"' tsconfig.check.json && grep -q '"noEmit": true' tsconfig.check.json && grep -q '"rootDir": "\."' tsconfig.check.json && grep -q 'remotion/\*\*/\*' tsconfig.check.json && grep -q '"rootDir": "\./src"' tsconfig.json && (! grep -q 'remotion/\*\*/\*' tsconfig.json) && grep -q '"typecheck": "tsc --noEmit -p tsconfig.check.json"' package.json && grep -q '"golazo": "\./dist/cli/index\.js"' package.json && grep -q '!remotion/assets/fonts/\*\.ttf' .gitignore && grep -q '!remotion/assets/music/\*\.mp3' .gitignore && grep -q '!remotion/assets/music/index\.json' .gitignore && grep -q 'SIL Open Font License' remotion/assets/fonts/README.md && npx tsc --noEmit -p tsconfig.check.json && npm run build && node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync('package.json','utf8')); const binPath=p.bin && p.bin.golazo; if(!binPath) { console.error('package.json#bin.golazo missing'); process.exit(1); } if(!fs.existsSync(binPath)) { console.error('bin target does not exist: '+binPath); process.exit(1); } console.log('bin resolves: '+binPath);" && (! git check-ignore -q remotion/assets/fonts/CormorantGaramond-Italic.ttf)</automated>
  </verify>
  <acceptance_criteria>
    - `remotion/assets/fonts/CormorantGaramond-Italic.ttf`, `remotion/assets/fonts/Inter-Regular.ttf`, `remotion/assets/fonts/Inter-Bold.ttf` exist, each >10kB, each `file`-detected as TrueType
    - `remotion/assets/fonts/README.md` declares SIL Open Font License 1.1, lists the three font files, and records the source URL used to fetch them
    - `tsconfig.check.json` exists at the project root with `"extends": "./tsconfig.json"`, `"noEmit": true`, `"rootDir": "."`, and `"include": ["src/**/*", "remotion/**/*"]`
    - `tsconfig.json` is UNCHANGED — `rootDir` still `"./src"`, `outDir` still `"./dist"`, `include` still `["src/**/*"]` (NO `remotion/**/*` added); `git diff tsconfig.json` shows zero lines changed
    - `package.json` `scripts.typecheck` is `"tsc --noEmit -p tsconfig.check.json"`; `package.json` `bin.golazo` is STILL `"./dist/cli/index.js"` (unchanged from Phase 1)
    - `npx tsc --noEmit -p tsconfig.check.json` exits 0 with the existing Phase 1 source tree intact (no remotion/ TS files yet — include is a no-op at this point, but the config itself parses)
    - `npm run build` still produces `./dist/cli/index.js`; the bin-resolution check `node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync('package.json','utf8')); if(!fs.existsSync(p.bin.golazo)) process.exit(1);"` exits 0 — proves the bin path still resolves after this task
    - `.gitignore` contains `!remotion/assets/fonts/*.ttf` AND `!remotion/assets/music/*.mp3` AND `!remotion/assets/music/index.json` exceptions under labelled blocks consistent with the existing `!tests/fixtures/...` idiom; `git check-ignore` confirms the committed TTFs and the future music-fixture paths are NOT ignored. NOTE: this plan ALSO emits the music whitelist so Plan 02-02 (parallel in Wave 1) does not have to touch `.gitignore` — keeping the two Wave-1 plans file-disjoint.
    - No edits to `package.json#bin`, `package.json#scripts.build`, `package.json#dependencies`, or `package.json#devDependencies` (only `scripts.typecheck` is touched in THIS task; Task 2 owns the dependency adds)
  </acceptance_criteria>
  <done>
    Font assets are committed, license is documented, the `npm run typecheck` script now covers BOTH `src/` and `remotion/`, AND the existing Phase 1 build still typechecks AND still emits the `bin`-referenced path `./dist/cli/index.js`. Plans 02-04 can rely on these files being on disk and on `tsconfig.check.json` covering remotion/ for type-checking.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement remotion/theme tokens + fonts + grade with table-driven unit tests</name>
  <files>remotion/theme/tokens.ts, remotion/theme/fonts.ts, remotion/theme/grade.ts, remotion/theme/index.ts, remotion/theme/tokens.test.ts, remotion/theme/grade.test.ts, package.json</files>
  <read_first>
    - docs/superpowers/specs/2026-05-13-golazo-design.md (sections: Architecture > top-level project layout; Open Items > slo-mo audio handling; Open Items > ChapterCard rhythm — informs MOTION constant defaults)
    - .planning/REQUIREMENTS.md (REN-03 — cinematic grade + typography)
    - remotion/assets/fonts/README.md (Task 1 — file names this task imports from)
    - tsconfig.check.json (Task 1 — confirms `remotion/**/*` is included so `npm run typecheck` sees these files; base `tsconfig.json` is unchanged)
    - Context7 `@remotion/fonts` docs — resolve-library-id with `remotion fonts`, then query-docs for "loadFont API 4.x browser/server registration" and "staticFile font URL". The exported API may be `loadFont` (single) or `loadFonts` (multiple) depending on version — pick the one available in the installed Remotion 4.x version and adapt this task's signatures accordingly. If the installed Remotion version pre-dates `@remotion/fonts`, fall back to the document-side `new FontFace(...).load()` pattern documented in `https://www.remotion.dev/docs/fonts` and register inside `loadFonts()`.
    - Context7 React `React.CSSProperties` type (for the grade-style return-type — confirm `filter` and `pointerEvents` strings are accepted as documented)
  </read_first>
  <behavior>
    Tokens (`remotion/theme/tokens.test.ts`):
    1. `COLORS.background === '#0a0a0a'`, `COLORS.foreground === '#f5f1e8'`, `COLORS.accentDefault === '#ffce5a'` — pinned defaults
    2. `TYPOGRAPHY.display.family === 'Cormorant Garamond'`, `.style === 'italic'`; `TYPOGRAPHY.label.family === 'Inter'`
    3. `TYPOGRAPHY.sizes.hero > TYPOGRAPHY.sizes.sub` (visual hierarchy assertion)
    4. `LAYOUT.width === 1920 && LAYOUT.height === 1080` (output resolution pinned)
    5. `MOTION.fps === 30 && MOTION.firstClipPlaybackRate === 0.5` (REN-01 slo-mo opener defaults)
    6. `MOTION.titleCardFrames > 0 && MOTION.outroFrames > 0` (sanity)
    7. Every key of `COLORS` matches `/^(#[0-9a-fA-F]{6}|rgba?\([^)]+\))$/` (every value is a valid CSS color string) — table-driven across `Object.entries(COLORS)`. Skip non-string nested keys.
    8. `COLORS`, `TYPOGRAPHY`, `LAYOUT`, `MOTION` are all `as const` — assertion: `Object.isFrozen(COLORS)` is false but writing to a key throws in strict mode TypeScript (compile-time check; runtime check uses `Object.getOwnPropertyDescriptor(...).writable === true` — `as const` is a TS-only assertion, so runtime defaults to mutable. The test asserts the TS type via a never-instantiated type-only check using `expectTypeOf` from vitest, OR by importing and asserting `typeof COLORS.background extends '#0a0a0a' ? true : false`. Use vitest's `expectTypeOf(COLORS.background).toEqualTypeOf<'#0a0a0a'>()` if available; otherwise gate compile-time only via `// @ts-expect-error` on a mutation attempt.)

    Grade helpers (`remotion/theme/grade.test.ts`):
    1. `getCinematicGradeStyle()` returns an object with a `filter` string matching `/saturate\([0-9.]+\)/`, `/contrast\([0-9.]+\)/`, AND `/brightness\([0-9.]+\)/` (all three filter functions present)
    2. `getCinematicGradeStyle()` is referentially the same object on repeated calls (memoized) OR deep-equal — choose memoized so React reconciliation does not re-create the style object; assert `getCinematicGradeStyle() === getCinematicGradeStyle()`
    3. `getVignetteOverlayStyle()` returns `{ position: 'absolute', inset: 0, pointerEvents: 'none', background: <radial-gradient>, ... }` — assertion on each documented key
    4. `getVignetteOverlayStyle().background` matches `/^radial-gradient\(/`
    5. Like #2, the vignette result is memoized: same object on repeated calls

    No unit tests for `fonts.ts` in this task. Reason: `loadFonts()` is a browser-side / Remotion-bundle-side call; mocking `@remotion/fonts` for a vitest run adds fragile machinery without signal. Plan 03's composition rendering will exercise font loading in the Remotion runtime context; Plan 04's renderStill integration test will pin actual rendered glyphs.
  </behavior>
  <action>
    --- package.json ---
    Add `@remotion/fonts` to `dependencies` at the latest 4.x version (resolve via `npm view @remotion/fonts version` if uncertain, then `npm install --save @remotion/fonts@^4`). If Context7's docs reveal that Remotion 4.x exposes font registration via the top-level `remotion` package (not `@remotion/fonts`), use that instead and skip the new dependency. Either way, also add `react` and `react-dom` at the latest 18.x or 19.x compatible with the chosen Remotion version (Remotion 4.x compositions need React at runtime; Plan 03 will create the .tsx composition files). Do NOT pin the exact version — use caret ranges (`^`) so the lock file resolves the current 4.x point release.

    Do NOT touch `package.json#bin`, `package.json#scripts.build`, or `package.json#scripts.typecheck` here — Task 1 already set the typecheck script; this task adds dependencies only.

    After the install, confirm the new dependencies appear in `package.json` and `package-lock.json` is updated.

    --- remotion/theme/tokens.ts ---
    Define and export `COLORS`, `TYPOGRAPHY`, `LAYOUT`, `MOTION` as TypeScript `as const` literal objects matching the `<interfaces>` block contents exactly. No imports, no runtime side effects. Inline TSDoc on each top-level export.

    --- remotion/theme/fonts.ts ---
    Imports: `loadFont` (or `loadFonts`) from `@remotion/fonts` (chosen per Context7 lookup); `staticFile` from `remotion` (this is the documented way to resolve `public/` and `remotion/assets/` URLs inside a composition). If `staticFile` cannot resolve `remotion/assets/fonts/...` (i.e. it only resolves `public/`), use a relative import URL: `import CormorantGaramondItalic from '../assets/fonts/CormorantGaramond-Italic.ttf?url'` if `?url` is supported by the bundler, OR fall back to `new URL('../assets/fonts/...', import.meta.url).href`.

    Define `FONT_FAMILIES` as documented in `<interfaces>`. Define and export `async function loadFonts(): Promise<{ display: string; label: string }>` that:
    1. Calls the chosen font registration API for the Cormorant Garamond Italic face: family `'Cormorant Garamond'`, weight `400`, style `'italic'`, URL from the staticFile / URL resolution above.
    2. Same for Inter Regular (family `'Inter'`, weight 400, style normal).
    3. Same for Inter Bold (family `'Inter'`, weight 700, style normal).
    4. Returns `{ display: FONT_FAMILIES.display, label: FONT_FAMILIES.label }`.
    5. The function is idempotent: wrap the body in a module-level `let loadOnce: Promise<...> | null = null;` cache. Repeated calls return the same Promise.

    --- remotion/theme/grade.ts ---
    Import `React` type only (`import type * as React from 'react'`) so the file stays runtime-React-free.

    Module-level memo: `let cinematicGradeStyle: React.CSSProperties | null = null;` and `let vignetteOverlayStyle: React.CSSProperties | null = null;`.

    Export `getCinematicGradeStyle(): React.CSSProperties` — returns the cached object or initializes it to `{ filter: 'saturate(1.12) contrast(1.05) brightness(0.96)' }` on first call.

    Export `getVignetteOverlayStyle(): React.CSSProperties` — returns the cached object or initializes it to:
      `{ position: 'absolute', inset: 0, pointerEvents: 'none', background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.6) 100%)' }`.

    Inline TSDoc explains the cinematic grade was tuned for soccer footage shot in mixed natural light: slight saturation bump, mild contrast lift, mild brightness drop to avoid blown highlights — Plan 03 spreads this over each Clip via `<div style={getCinematicGradeStyle()}>`.

    --- remotion/theme/index.ts ---
    Single-file barrel:
    ```
    export * from './tokens.js';
    export * from './fonts.js';
    export * from './grade.js';
    ```

    --- remotion/theme/tokens.test.ts ---
    Implement all 8 cases above. Use `describe('theme tokens', ...)`. For case 7, build a `[name, value]` table from `Object.entries(COLORS)` and run a table-driven `it.each` assertion. For case 8, prefer `expectTypeOf` from vitest 4.x; if not available, write a type-level check at the bottom of the file:
    ```
    // Compile-time-only assertion that tokens are `as const`.
    // @ts-expect-error - mutation should be rejected at the type layer
    COLORS.background = 'oops';
    ```
    Wrap the `// @ts-expect-error` line in `if (false) { ... }` so it never executes at runtime; if TypeScript stops rejecting it (i.e. tokens are no longer `as const`), `tsc --noEmit` fails.

    --- remotion/theme/grade.test.ts ---
    Implement all 5 cases. Use simple `describe('cinematic grade helpers', ...)`.

    RED → GREEN: write the tests first, watch them fail, implement.
  </action>
  <verify>
    <automated>cd /Users/me/Documents/code/golazo && npm install && npx tsc --noEmit -p tsconfig.check.json && npx vitest run remotion/theme/tokens.test.ts remotion/theme/grade.test.ts && grep -q "export const COLORS" remotion/theme/tokens.ts && grep -q "export const TYPOGRAPHY" remotion/theme/tokens.ts && grep -q "export const LAYOUT" remotion/theme/tokens.ts && grep -q "export const MOTION" remotion/theme/tokens.ts && grep -q "export async function loadFonts" remotion/theme/fonts.ts && grep -q "export function getCinematicGradeStyle" remotion/theme/grade.ts && grep -q "export function getVignetteOverlayStyle" remotion/theme/grade.ts && grep -q "export \* from './tokens" remotion/theme/index.ts && npx tsx -e "import('./remotion/theme/index.js').then(m => { if (!m.COLORS || !m.TYPOGRAPHY || !m.LAYOUT || !m.MOTION || !m.loadFonts || !m.getCinematicGradeStyle || !m.getVignetteOverlayStyle) { console.error('barrel re-exports incomplete'); process.exit(1); } })" && npm run build && node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync('package.json','utf8')); if(!fs.existsSync(p.bin.golazo)) { console.error('bin path '+p.bin.golazo+' missing after build'); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `remotion/theme/tokens.ts` exports `COLORS`, `TYPOGRAPHY`, `LAYOUT`, `MOTION` matching the pinned values in `<interfaces>`; 8 unit-test cases pass
    - `remotion/theme/fonts.ts` exports `FONT_FAMILIES` and `async loadFonts()`; idempotent via module-level promise cache; loads Cormorant Garamond Italic + Inter Regular + Inter Bold from the committed TTFs under `remotion/assets/fonts/`
    - `remotion/theme/grade.ts` exports `getCinematicGradeStyle()` returning `{ filter: '...saturate...contrast...brightness...' }` and `getVignetteOverlayStyle()` returning a radial-gradient overlay style; both memoized (same object on repeated calls); 5 unit-test cases pass
    - `remotion/theme/index.ts` barrel re-exports all of the above; `npx tsx -e "import('./remotion/theme/index.js')"` resolves without runtime error
    - `package.json` lists `@remotion/fonts` (or the equivalent font-registration API documented for the chosen Remotion 4.x point release) and React 18.x or 19.x compatible with that Remotion version as dependencies
    - `package.json#bin.golazo` is STILL `./dist/cli/index.js` (unchanged); `npm run build` after this task still produces that path and the bin-resolution check passes
    - `npx tsc --noEmit -p tsconfig.check.json` exits 0 with the new `remotion/theme/` tree included
    - `npx vitest run remotion/theme/tokens.test.ts remotion/theme/grade.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Theme primitives exist, are unit-tested, and importable via the `remotion/theme` barrel. Plan 03 (compositions) will spread the grade + vignette helpers onto Episode/Clip layers and pull typography + color tokens from this barrel; Plan 04 (render driver) will call `loadFonts()` before bundling. REN-03 is in flight; final verification arrives when Plan 03's snapshot tests render actual glyphs with these fonts. The Phase 1 CLI bin path (`./dist/cli/index.js`) remains intact.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p tsconfig.check.json` passes against the union of `src/` (Phase 1) + new `remotion/theme/` files
- `npm run build` (which uses the base `tsconfig.json` with `rootDir: "./src"`) still emits `dist/cli/index.js` at the path referenced by `package.json#bin.golazo`; `node -e "require('fs').existsSync(require('./package.json').bin.golazo)"` is `true`
- `npx vitest run remotion/theme/` passes the tokens + grade unit suites (no fonts unit test; covered by Plan 03's composition integration)
- Three TTF files are committed under `remotion/assets/fonts/` and confirmed-not-gitignored
- `remotion/theme/index.ts` barrel resolves all named exports via `npx tsx -e "import('./remotion/theme/index.js')"` smoke
- `package.json` includes `@remotion/fonts` and react/react-dom; `package-lock.json` updated
- `tsconfig.json` is byte-identical to its pre-plan state (verified by `git diff tsconfig.json` showing no changes)
- `tsconfig.check.json` exists at the project root with the documented extends+rootDir+include shape
</verification>

<success_criteria>
Plan complete when:
- All files in `files_modified` exist; `npm run typecheck` (== `tsc --noEmit -p tsconfig.check.json`), `npx vitest run remotion/theme/`, and `npm run build` (which still produces `dist/cli/index.js`) all pass
- REN-03 visual-primitives surface is in place: typography is self-hosted, color/layout/motion tokens are exported as constants, cinematic grade + vignette helpers return inline-style objects
- Plan 02 (music picker, parallel in Wave 1) does NOT import from `remotion/theme` — confirms no accidental cross-plan coupling broke Wave 1 parallelism
- Plans 03 + 04 have a stable theme surface to depend on (Plan 03 in Wave 2, Plan 04 in Wave 3)
- Phase 1's `package.json#bin.golazo` -> `./dist/cli/index.js` invariant is preserved (no rootDir migration, no bin path shift)
</success_criteria>

<output>
After completion, create `.planning/phases/02-render-pipeline/02-01-SUMMARY.md` recording:
- The chosen Remotion font-registration API (e.g. `@remotion/fonts.loadFont` vs the legacy `new FontFace` pattern) and the version of `@remotion/fonts` / `remotion` actually installed
- The actual download URLs for the three TTFs (so future regen / license audits do not have to re-discover them)
- The cinematic-grade filter string (saturate/contrast/brightness exact values) — Plan 03 + Plan 04 must NOT change these silently
- Any deviation from the pinned MOTION constants (e.g. if Remotion 4.x in this project happens to be 60fps elsewhere — flag it so Plan 03 + Plan 04 know)
- ARCHITECTURAL CONFIRMATION (load-bearing for Plan 02-04): the base `tsconfig.json` was NOT modified — `rootDir: "./src"`, `outDir: "./dist"`, `include: ["src/**/*"]` unchanged. The new `tsconfig.check.json` extends it for `npm run typecheck` to cover `remotion/`. `npm run build` still emits `./dist/cli/index.js` at the path `package.json#bin.golazo` references. Remotion's own bundler (`@remotion/bundler`'s `bundle({ entryPoint: 'remotion/Root.tsx' })`) compiles TSX directly; `tsc` emit is not in the Remotion bundle pipeline, so we never need `tsc` to emit anything under `remotion/`.
</output>

---
phase: 04-convenience-qa-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - src/cli/all.integration.test.ts
  - src/prepare/index.test.ts
  - src/render/driver.test.ts
  - src/publish/runner.test.ts
  - src/cli/publish.integration.test.ts
  - tests/integration/qa-audit.test.ts
autonomous: true
requirements:
  - QA-01
  - QA-02

must_haves:
  truths:
    - "`npm run test:coverage` exits 0 — line coverage on `src/` ≥ 80% per the threshold installed in Plan 04-02"
    - "Table-driven unit tests EXIST for the filename parser (src/prepare/filename.test.ts), channels loader (src/config/channels.test.ts), music-picker determinism (src/render/musicPicker.test.ts), and title+description renderers (src/publish/templates.test.ts) — verified by a single audit test that imports each test-cases file and asserts the case-count constants are >= the minimums set in prior phases"
    - "Integration test EXISTS that runs `prepare` end-to-end against the committed `tests/fixtures/golazo/leo/2026-05-13_vs_united_3-1/` fixture and asserts the resulting manifest matches the documented schema (existing src/prepare/index.test.ts cases 11-13 + any added)"
    - "Integration test EXISTS that runs `render` end-to-end at `--low-res` against the same fixture and asserts `.golazo/episode.mp4` exists with a non-zero file size and ffprobe-confirmed duration > 0 (existing src/render/driver.test.ts + Plan 04-01's all.integration.test.ts cover this)"
    - "Integration test EXISTS that runs `publish` against nock-stubbed YouTube and asserts publish.json is written with `privacyStatus: 'unlisted'` AND request body shape (existing src/publish/runner.test.ts + src/cli/publish.integration.test.ts cover this)"
    - "The `tests/integration/qa-audit.test.ts` test enumerates the four QA-01 unit-test files by path, imports the table-driven case array constants from their sibling `*.test-cases.ts` files where they exist, and fails loudly if any case array is empty or below the minimum count documented in prior plans (filename: >= 8, channels: >= 6, music-picker: >= 5, templates: >= 6)"
    - "Per-module coverage breakdown documented: any `src/` file under 80% individual-file line coverage is identified in the plan SUMMARY with the explanation (e.g. error path not exercisable, branch reachable only via real Google OAuth)"
    - "No new heavy mocks are introduced — the integration suite reuses the existing nock + GOLAZO_OAUTH_MOCK shell-out pattern from Plans 03-03/03-05 and the in-process Remotion file-server pattern from Plan 02-04"
  artifacts:
    - path: "tests/integration/qa-audit.test.ts"
      provides: "Single audit test asserting the four QA-01 unit-test files exist and their table-driven case counts meet the minimums established by Plans 01-02, 01-03, 02-02, and 03-02"
      exports: []
    - path: "src/cli/all.integration.test.ts"
      provides: "(if needed) Additional cases beyond Plan 04-01's 6 cases to close any coverage gap on src/cli/all.ts uncovered by the 04-01 suite"
      exports: []
  key_links:
    - from: "tests/integration/qa-audit.test.ts"
      to: "src/prepare/filename.test-cases.ts"
      via: "imports the FILENAME_TEST_CASES const + asserts length"
      pattern: "FILENAME_TEST_CASES"
    - from: "tests/integration/qa-audit.test.ts"
      to: "src/config/channels.test-cases.ts"
      via: "imports CHANNELS_TEST_CASES + asserts length"
      pattern: "CHANNELS_TEST_CASES"
    - from: "tests/integration/qa-audit.test.ts"
      to: "src/publish/templates.test-cases.ts"
      via: "imports templates test cases + asserts length"
      pattern: "TEMPLATES_TEST_CASES|TITLE_TEST_CASES|DESCRIPTION_TEST_CASES"
---

<objective>
Close QA-01 and QA-02 (both halves) by:

1. **Audit-verify** the four mandated QA-01 table-driven unit-test files already exist (they do, from Plans 01-02 / 01-03 / 02-02 / 03-02) and their case-count constants meet the documented minimums.
2. **Measure** the current `npm run test:coverage` line coverage on `src/` against the 80% threshold installed in Plan 04-02.
3. **Close the gap** if coverage is under 80%: add fill-in integration test cases targeting under-covered modules. Prefer extending existing integration test files over creating new mocks.
4. **Document** per-module coverage and any rational exclusions in the plan SUMMARY.

Purpose: Locks in the QA-01 (table-driven unit tests) and QA-02 (integration tests + ≥ 80% coverage) requirements with a single CI-checkable command (`npm run test:coverage`).

Output: `npm run test:coverage` exits 0; the QA audit test passes; per-module coverage documented.

This plan is the largest of the four — task sizing is intentional because the work is bounded by the actual measured gap (which may be zero if the existing 367 + Plan 04-01's tests already exceed 80%).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-prepare-pipeline/01-02-SUMMARY.md
@.planning/phases/01-foundation-prepare-pipeline/01-03-SUMMARY.md
@.planning/phases/02-render-pipeline/02-02-SUMMARY.md
@.planning/phases/03-publish-pipeline/03-02-SUMMARY.md
@.planning/phases/04-convenience-qa-polish/04-01-SUMMARY.md
@.planning/phases/04-convenience-qa-polish/04-02-SUMMARY.md
@docs/superpowers/specs/2026-05-13-golazo-design.md
@vitest.config.ts
@src/prepare/filename.test-cases.ts
@src/config/channels.test-cases.ts
@src/publish/templates.test-cases.ts

<interfaces>
<!-- Existing test-cases constants (paths + names). Verified to exist as of Phase 3 close. -->

- src/prepare/filename.test-cases.ts exports a const array of test cases (Plan 01-03 minimum: 8 cases — valid + 7 malformed variants).
- src/config/channels.test-cases.ts exports CHANNELS_TEST_CASES (Plan 01-02 minimum: 6 cases — see SUMMARY).
- src/prepare/kid.test-cases.ts exports kid-resolver cases (Plan 01-03).
- src/publish/templates.test-cases.ts exports template renderer cases (Plan 03-02 — verify name; could be TEMPLATE_TEST_CASES or split into TITLE_/DESCRIPTION_).
- src/render/musicPicker.test.ts has a 5-case determinism block (no sibling test-cases.ts file per Plan 02-02 SUMMARY — the cases are inline). Audit test handles this by checking the test count via a different mechanism (grep or describe-block name match) OR by accepting that determinism cases are inline.

<!-- Coverage thresholds from Plan 04-02: vitest.config.ts coverage.thresholds.lines: 80 over include: ['src/**/*.ts']. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QA-01 audit test — assert the four mandated test-cases files exist with adequate case counts</name>
  <files>tests/integration/qa-audit.test.ts</files>
  <read_first>
    - src/prepare/filename.test-cases.ts (find the exported const name and current length)
    - src/config/channels.test-cases.ts (find the exported const name CHANNELS_TEST_CASES)
    - src/publish/templates.test-cases.ts (find the exported const name(s))
    - src/render/musicPicker.test.ts (confirm the determinism block has ≥ 5 it() cases — used as the music-picker proof since cases are inline per Plan 02-02 SUMMARY)
    - .planning/phases/01-02-SUMMARY.md, .planning/phases/01-03-SUMMARY.md, .planning/phases/02-02-SUMMARY.md, .planning/phases/03-02-SUMMARY.md (find the documented minimum case counts to assert)
  </read_first>
  <action>
    Create tests/integration/qa-audit.test.ts. The audit test is a single vitest file with one describe block named `QA-01 table-driven coverage audit`. Inside:

    1. Import the four case-array constants from their `*.test-cases.ts` siblings (filename, channels, templates). For music-picker, since cases are inline rather than in a test-cases sibling, do NOT import — instead use Node's `readFileSync` on `src/render/musicPicker.test.ts` and count `it(`/`test(` occurrences inside the determinism describe block.

    2. Per case array, an `it(...)` block asserts `array.length >= MIN`. Minimums (sourced from prior plan SUMMARIES — verify the exact numbers when reading):
       - filename: >= 8 (valid + at least 7 malformed)
       - channels: >= 6
       - templates: >= 6
       - musicPicker determinism: >= 5 it() blocks in the relevant describe

    3. A final `it('all four QA-01 test files exist', ...)` block asserts existsSync on the four test file paths: `src/prepare/filename.test.ts`, `src/config/channels.test.ts`, `src/render/musicPicker.test.ts`, `src/publish/templates.test.ts`.

    4. If the inline music-picker count cannot be reliably extracted via regex (false positives from comments), fall back to importing the test file with a side-effect-free read and counting `\bit\(` matches outside of `// ` lines. Keep the regex simple — `/^\s*it\(/gm` against the file contents is sufficient because the codebase uses one-per-line `it(...)` declarations.

    5. The audit test MUST fail with a clear message if any minimum is unmet — e.g. `expect(filenameCases.length, 'QA-01: filename test cases below minimum (8); update src/prepare/filename.test-cases.ts').toBeGreaterThanOrEqual(8)`.

    Place the file at tests/integration/qa-audit.test.ts (creates the tests/integration/ directory). This path is already covered by the existing vitest `test.include` glob `tests/**/*.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/qa-audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/qa-audit.test.ts exists
    - All audit it() blocks pass: `npx vitest run tests/integration/qa-audit.test.ts` exits 0
    - The audit imports at least 3 *.test-cases.ts files (filename, channels, templates)
    - `grep -c "toBeGreaterThanOrEqual" tests/integration/qa-audit.test.ts` >= 4
    - `grep -c "existsSync" tests/integration/qa-audit.test.ts` >= 1
  </acceptance_criteria>
  <done>QA-01 audit test green; documented minimums asserted against prior phases' table-driven case arrays</done>
</task>

<task type="auto">
  <name>Task 2: Measure baseline coverage and close gaps to ≥ 80% on src/</name>
  <files>src/cli/all.integration.test.ts, src/prepare/index.test.ts, src/render/driver.test.ts, src/publish/runner.test.ts, src/cli/publish.integration.test.ts</files>
  <read_first>
    - .planning/phases/04-convenience-qa-polish/04-02-SUMMARY.md (baseline coverage number from Plan 04-02's measurement run)
    - The post-`npm run test:coverage` text output (run it first as a measurement step before writing any test code)
    - Each under-covered `src/` file identified by the baseline run
    - src/cli/publish.integration.test.ts (existing shell-out pattern — extend rather than create new)
    - src/cli/all.integration.test.ts (created in Plan 04-01 — extend rather than create new)
  </read_first>
  <action>
    Step 1 (MEASURE FIRST — do not write code blindly): run `npm run test:coverage`. Capture the per-file line coverage table from the text reporter output. Identify every `src/**/*.ts` file under 80%.

    Step 2 (PRIORITIZE by impact): rank under-covered files by `(lines_uncovered) / (file_lines)`. Largest absolute uncovered-line counts get fill-in tests first because they move the global percentage the most.

    Step 3 (DECIDE per file):

    For each under-covered file, choose ONE of three actions:

    A) **Add a fill-in test case** in the appropriate existing test file. Prefer integration over unit — most uncovered lines as of Phase 3 close are in orchestrator branches (idempotency paths, force paths, error remediation paths) which are exercised more cleanly by shell-out integration tests than by mocks.

    B) **Add a rational exclude** to vitest.config.ts `coverage.exclude` IF the file is genuinely type-only or test-helper-only. Document the exclusion in the plan SUMMARY with a one-line justification per file. Examples that may qualify: `src/prepare/types.ts`, `src/config/types.ts`, `src/publish/index.ts` (barrel file with no logic — but the executor must confirm by reading it; if it has runtime exports beyond re-exports, do NOT exclude).

    C) **Document as a known gap** in the plan SUMMARY with an `// istanbul ignore next` or `/* v8 ignore next */` comment AND a SUMMARY entry explaining why the branch is uncoverable (e.g. catches a Google SDK error class that can only be triggered against the real API). Use this sparingly — prefer A or B.

    Step 4 (WRITE THE FILL-INS):

    Fill-in tests must be additive — they extend existing describe blocks, not new test files. New test files create more file-discovery cost without adding signal. Common gap-closure patterns:

    - **Error remediation strings** in `src/publish/errors.ts` / `src/prepare/errors.ts`: add a test asserting `new ErrorClass({...}).message` contains the documented remediation. These are usually 100% coverable via one it() per error class.
    - **Idempotency `--force` branches** in src/prepare/index.ts, src/render/driver.ts, src/publish/runner.ts: existing tests may exercise the non-force path only. Add a force-path it() that calls `runX({ ..., force: true })` against a folder that already has the prior-stage output. Reuse the existing tmpdir fixture-copy helper.
    - **CLI error formatting** in src/cli/commands/*.ts: existing CLI integration tests may not exercise the catch-block in every handler. Add a shell-out it() that triggers a known error (e.g. malformed folder name) and asserts stderr contains the original message AND the CommanderError exit code.
    - **AllStageError construction paths** in src/cli/all.ts (Plan 04-01): if the unit test coverage missed an option-forwarding branch (e.g. lowRes default false), add an it() that asserts the default propagation.

    Step 5 (VERIFY):

    Re-run `npm run test:coverage`. Confirm:
    - Total line coverage on `src/` ≥ 80%
    - Per-file: no file dropped to LOWER coverage than the baseline (regression gate — adding tests shouldn't decrease per-file coverage)
    - All previously passing tests still pass
    - The audit test from Task 1 still passes

    If after one fill-in pass the threshold is still missed, run a SECOND pass on the next-biggest uncovered file. Do NOT artificially inflate coverage by testing trivial getters or by removing exclusions to game the denominator.

    Stop signal: `npm run test:coverage` exits 0 (threshold met). Do not over-engineer beyond 80% — the v1 goal is the gate, not a saturation target.
  </action>
  <verify>
    <automated>npm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:coverage` exits 0 (line coverage on `src/` ≥ 80%)
    - Per-module breakdown is captured in the plan SUMMARY with any rational excludes or `v8 ignore` comments justified
    - All 367 prior tests + new audit test + Plan 04-01's new tests still pass — `npx vitest run` exits 0
    - `npx tsc --noEmit -p tsconfig.check.json` exits 0
    - No `vi.mock` calls added against any `src/render/driver.ts` or `src/publish/uploader.ts` — orchestrator tests stay shell-out + nock; mocks at the orchestrator layer would mask real wiring bugs (this is enforced via spot-check: `grep -c "vi.mock.*'\\.\\./driver" src/` returns 0)
    - If any v8-ignore comments are added, they are accompanied by an inline `// reason: ...` comment AND a SUMMARY entry — `grep -c "v8 ignore" src/` matches `grep -c "reason:" src/ | xargs` (every ignore has a reason; trivial pattern check)
  </acceptance_criteria>
  <done>npm run test:coverage exits 0; per-module gap closure documented; no orchestrator-layer mocks introduced; ≥ 80% line coverage on src/ proven by CI-style command</done>
</task>

</tasks>

<verification>
- `npm run test:coverage` exits 0 from a fresh clone.
- `npx vitest run tests/integration/qa-audit.test.ts` exits 0 — the four QA-01 table-driven test files exist with adequate cases.
- `npx tsc --noEmit -p tsconfig.check.json` exits 0.
- All Phase 1/2/3/04-01 tests still pass; no regressions in any sub-stage.
- QA-01 satisfied: table-driven unit tests for filename parser, channels loader, music-picker determinism, title/description renderers all exist and audit-verified.
- QA-02 satisfied: integration tests cover prepare-against-fixture, low-res render end-to-end, publish-with-nock — AND `src/` line coverage ≥ 80%.
</verification>

<success_criteria>
- QA audit test passes
- npm run test:coverage exits 0 (≥ 80% lines on src/)
- Plan SUMMARY documents per-module coverage breakdown and any rational excludes
- No regressions in prior phases' tests
- QA-01 + QA-02 both closed
</success_criteria>

<output>
After completion, create `.planning/phases/04-convenience-qa-polish/04-03-SUMMARY.md` recording:
- Final line coverage % on src/
- Per-file coverage table (text reporter output, formatted as a markdown table)
- Any v8-ignore comments added (file + line + reason)
- Any vitest.config.ts coverage.exclude additions (file + justification)
- Number of fill-in test cases added (per existing test file)
- Confirmation that no orchestrator-layer mocks were introduced
- Hand-off note for Plan 04-04: snapshot tests will add ~2 test files; they are NOT subject to the 80% threshold (excluded under tests/ pattern) and do not affect src/ coverage
</output>

# Continue Here — golazo

**Paused:** 2026-06-02T16:55:00Z
**Status:** Between milestones — v2.0 just shipped + tagged
**No active phase.** Both v1.0 (CLI) and v2.0 (Web UI) are archived and feature-complete.

---

## Where You Left Off

Just shipped **v2.0 — Web UI** (commit `8f83155`, tag `v2.0`):
- 4 phases (5-8), 16 plans, 22/22 reqs closed, 1 override (WEB-QA-03 live run)
- 609 tests passing (403 root + 204 web unit + 2 Playwright E2E specs)
- 95% web line coverage (gate 80%)

Just shipped **v1.0 — MVP CLI** prior (commit pre-`bf99c11`, tag `v1.0`):
- 4 phases (1-4), 18 plans, 28/28 reqs closed, 1 override (PUB-05 multipart)
- 387 root tests, 86.72% line coverage

## Current State of the Repo

- Working tree clean (post-archive commit)
- `.planning/` only contains active milestone scaffolding — current ROADMAP.md is collapsed two-milestone summary + v2.1+ backlog; REQUIREMENTS.md removed (next milestone starts fresh)
- All per-plan SUMMARY.md + per-phase VERIFICATION.md committed
- Git tags: `v1.0`, `v2.0`. No remote configured

## Build + Test Surface

| Command | Purpose |
|---------|---------|
| `npm test` (root) | 403 root vitest tests |
| `cd web && npm test` | 204 web unit + integration tests (4 suites skippable via env) |
| `npm run test:coverage` (root) | v1.0 coverage gate (86.72%) |
| `cd web && npm run test:coverage` or root `npm run web:coverage` | v2.0 coverage gate (95%, ≤10 excludes, ≥60% surface floor) |
| `npm run build` (root) | CLI `tsc -p .` → `dist/cli/index.js` |
| `cd web && npm run build` | Next.js production build |
| `npm run typecheck` (root) | `tsc --noEmit -p tsconfig.check.json` — **7 pre-existing errors remain** (v1.0-typecheck-debt) |
| `npm run web:dev` | localhost web UI at 127.0.0.1:4173 |
| `npm run web:e2e` | Playwright (after `npx playwright install chromium`) — never run live yet |
| `golazo prepare/render/publish/auth/all <folder>` | CLI subcommands (after `npm run build` + add `dist/cli/index.js` to PATH or `npx golazo`) |

## What Needs Human Eyes Before Next Step

1. **Real-world smoke** — drop a real game folder under `~/golazo/leo/<YYYY-MM-DD>_vs_<slug>_<for>-<against>/`, run `golazo all`, watch unlisted upload appear in YouTube Studio, flip public manually. Until done, pipeline is theoretical
2. **Playwright live run** — `npx playwright install chromium && npm run web:e2e` from root. Two specs (golden-path + path-traversal) should pass against fixture-backed dev server
3. **Git remote** — `git remote add origin <url> && git push -u origin main --tags` to publish (or `gh repo create golazo --private --source=. --push --tags`)
4. **8 outstanding stashes** — `git stash list` to review; each phase boundary captured pre-existing src/web mods. Decide merge or drop

## v2.1+ Backlog (10 items)

**v2.0 carry-forwards:**
- **WEB-E2E-LIVE-RUN** — operator-side smoke before any public flip
- **WEB-VISUAL-REGRESSION** — pixelmatch snapshots of `/` + detail page
- **WEB-E2E-CROSS-BROWSER** — Firefox + Webkit Playwright projects
- **WEB-TRIGGER** — render/publish from browser with real-time progress
- **WEB-EDIT** — override title/description/score before publish
- **WEB-LAN** — LAN-accessible with shared-token auth

**v1.0 carry-forwards (still open):**
- **v1.0-typecheck-debt** — 7 tsc errors in src/cli/all.test.ts + src/publish/oauth.test.ts + retry.test.ts + runner.test.ts (vitest tolerates, tsc does not)
- **PUB-05-resumable** — googleapis SDK uses multipart not resumable upload session protocol
- **OAuth-DI** — replace `GOLAZO_OAUTH_MOCK=1` env-var shim in `exchangeCode` with injectable factory
- **QA-03-extended** — commit 6+-clip fixture + integration render for visual chapter-rhythm regression

## How to Resume

Pick one based on what's important:

### Path A: scope v2.1
```
/gsd-new-milestone
```
Triage v2.1+ backlog items into the next active milestone. Recommended scope if continuing development: WEB-E2E-LIVE-RUN + v1.0-typecheck-debt + WEB-VISUAL-REGRESSION are the cheapest closure of accumulated debt.

### Path B: real-world validation first
```bash
# 1. Build the CLI
npm run build

# 2. Auth one channel (interactive OAuth — needs YouTube Data API credentials in env)
golazo auth leo

# 3. Drop a real clip folder under ~/golazo/leo/<game>/
# 4. Run end-to-end
golazo all ~/golazo/leo/<game>

# 5. Browse via web
npm run web:dev
# Open http://127.0.0.1:4173 — verify episode appears, plays, has correct metadata

# 6. Flip unlisted → public in YouTube Studio manually
```

### Path C: clean up before continuing
```bash
git stash list                              # review 8 stashes
git stash show stash@{0} | less             # inspect each
git stash pop stash@{0}                     # if useful, merge
# OR
git stash drop stash@{0}                    # if obsolete, drop

# Then fix v1.0-typecheck-debt
npm run typecheck                           # see the 7 errors
# Fix one-by-one in src/cli/all.test.ts + src/publish/*.test.ts
npm test && npm run typecheck               # both must exit 0
```

### Path D: publish to remote
```bash
gh repo create golazo --private --source=. --push --tags
# Or
git remote add origin git@github.com:<owner>/golazo.git
git push -u origin main --tags
```

## Files to Read on Resume

If picking up cold, start with:
1. `.planning/PROJECT.md` — current state + key decisions + next milestone goals
2. `.planning/ROADMAP.md` — shipped milestones + v2.1+ backlog
3. `.planning/milestones/v2.0-ROADMAP.md` — most recent shipped milestone history
4. `.planning/HANDOFF.json` — structured machine-readable state

Per-phase SUMMARY.md files under `.planning/phases/01-04/` (v1.0) and `.planning/phases/05-08/` (v2.0) have implementation details if a v2.1 plan needs to reference v1.0/v2.0 patterns.

## Critical Anti-Patterns

None blocking. Resumption is unconstrained — pick any of A/B/C/D above.

---
*Generated 2026-06-02 by /gsd-pause-work after v2.0 milestone close.*
